# 第六节“实验结果、分析和结论”初级方案

## 一、章节定位

本节用于集中回答三个问题：

1. 本实验最终取得了什么定量结果。
2. 分类模型和分割模型的结果说明了什么。
3. 当前方法还有哪些不足，后续可以如何改进。

写作时应坚持使用已经真实运行得到的指标和图表，不补造实验结果。第六节不再重复大量训练流程细节，而是重点解释结果、比较模型、分析错误原因，并给出结论。

## 二、建议章节结构

建议将第六节拆成以下五个小节：

```latex
\section{实验结果、分析和结论}
\subsection{分类结果}
\subsection{分割结果}
\subsection{轻量消融实验}
\subsection{失败案例分析}
\subsection{结论与改进方向}
```

其中，“分类结果”和“分割结果”负责展示核心成果；“轻量消融实验”用于说明 ResNet18 与 EfficientNet-B0 的对比价值；“失败案例分析”用于体现对结果的理解；“结论与改进方向”用于总结实验收获。

## 三、分类结果部分

### 1. 建议放置内容

分类任务应说明本实验完成了 37 类宠物品种分类，并在官方 test split 上评估 ResNet18 baseline 和 EfficientNet-B0 主模型。

建议展示一张分类结果表：

| 模型 | Accuracy | Macro-F1 | Weighted-F1 | Top-5 Accuracy |
| --- | ---: | ---: | ---: | ---: |
| ResNet18 | 0.883074 | 0.881759 | 0.882539 | 0.986645 |
| EfficientNet-B0 | 0.899700 | 0.898173 | 0.898878 | 0.995639 |

建议配图：

- `outputs/figures/fig_cls_training_curve.png`：分类训练曲线。
- `outputs/figures/fig_confusion_matrix_efficientnet_b0.png` 或 `outputs/figures/fig_confusion_matrix.png`：EfficientNet-B0 混淆矩阵。
- `outputs/figures/fig_cls_correct_examples.png`：分类正确样例。
- `outputs/figures/fig_failure_cases.png`：分类失败样例。

### 2. 分析重点

可重点写以下结论：

- EfficientNet-B0 的 Accuracy 和 Macro-F1 均高于 ResNet18，说明主模型整体分类能力更强。
- EfficientNet-B0 相比 ResNet18 的 Accuracy 提升约 0.016626，Macro-F1 提升约 0.016414，提升幅度不算极大，但在 37 类细粒度分类任务上具有实际意义。
- 两个模型 Top-5 Accuracy 都很高，说明很多 Top-1 错误并不是完全无关的错误，而是模型通常仍能把真实类别排在候选类别前列。
- Macro-F1 与 Accuracy 接近，说明模型没有只偏向少数大类，整体类别表现较均衡。

### 3. 可写入正文的初级表述

分类结果表明，EfficientNet-B0 在官方测试集上取得 Accuracy 0.899700、Macro-F1 0.898173，均高于 ResNet18 的 Accuracy 0.883074 和 Macro-F1 0.881759。由于 Oxford-IIIT Pet 包含 37 个宠物品种，且部分类别之间外观差异较细，EfficientNet-B0 的提升说明更强的特征提取结构能够改善细粒度分类表现。两个模型的 Top-5 Accuracy 均接近或超过 0.98，说明模型在多数样本上已经能够将真实类别排入较靠前的候选范围。

## 四、分割结果部分

### 1. 建议放置内容

分割任务应说明本实验将 Oxford-IIIT Pet 的 trimap 转换为宠物前景与背景二分类 mask，并使用 DeepLabV3-MobileNetV3 进行前景语义分割。

建议展示一张分割结果表：

| 模型 | Pixel Accuracy | Background IoU | Foreground IoU | mIoU | Dice |
| --- | ---: | ---: | ---: | ---: | ---: |
| DeepLabV3-MobileNetV3 | 0.961981 | 0.936630 | 0.913212 | 0.924921 | 0.954638 |

建议配图：

- `outputs/figures/fig_seg_training_curve.png`：分割训练曲线。
- `outputs/figures/fig_seg_predictions.png`：分割预测可视化。
- 如需要，可补充低 mIoU 样例表，来源为已有分割失败分析结果。

### 2. 分析重点

可重点写以下结论：

- Pixel Accuracy 达到 0.961981，说明整体像素分类准确率较高。
- mIoU 为 0.924921，Dice 为 0.954638，说明预测 mask 与真实 mask 的重合程度较好。
- 前景 IoU 为 0.913212，低于背景 IoU 0.936630，说明宠物边界、毛发和局部细节仍比背景区域更难分割。
- Dice 高于 mIoU 是正常现象，因为二者定义不同，Dice 对区域重合的表达方式与 IoU 不完全相同。

### 3. 可写入正文的初级表述

分割结果表明，DeepLabV3-MobileNetV3 在官方测试集上取得 Pixel Accuracy 0.961981、mIoU 0.924921 和 Dice 0.954638。模型能够较稳定地分离宠物前景与背景，预测结果在大多数样本中能覆盖宠物主体区域。前景 IoU 低于背景 IoU，说明错误更多集中在宠物边界、毛发、遮挡和前景背景颜色接近的区域。

## 五、轻量消融实验部分

### 1. 建议放置内容

本项目的轻量消融建议只保留 ResNet18 与 EfficientNet-B0 的分类对比，不再额外展开 U-Net、Grad-CAM 或分割辅助分类，避免报告主线发散。

建议展示一张对比表：

| 模型 | 最佳 epoch | 验证 Macro-F1 | 测试 Accuracy | 测试 Macro-F1 | 训练总耗时 |
| --- | ---: | ---: | ---: | ---: | ---: |
| ResNet18 | 15 | 0.930059 | 0.883074 | 0.881759 | 765.40 s |
| EfficientNet-B0 | 13 | 0.935778 | 0.899700 | 0.898173 | 1010.81 s |

### 2. 分析重点

可重点写以下内容：

- EfficientNet-B0 测试集指标更高，说明其作为主分类模型是合理的。
- EfficientNet-B0 训练总耗时高于 ResNet18，说明性能提升伴随一定计算成本。
- ResNet18 结构简单、训练较快，适合作为 baseline；EfficientNet-B0 精度更高，适合作为主模型。
- 消融分析的结论应服务于“为什么选择 EfficientNet-B0 作为分类主模型”。

### 3. 可写入正文的初级表述

轻量消融实验表明，在相同数据划分、相同输入尺寸和相同测试集条件下，EfficientNet-B0 的验证集 Macro-F1 和测试集 Macro-F1 均高于 ResNet18。与此同时，EfficientNet-B0 的训练总耗时约为 1010.81 秒，高于 ResNet18 的 765.40 秒。因此，EfficientNet-B0 的性能提升是以一定训练时间成本换来的；综合分类效果和实验目标，本实验将 EfficientNet-B0 作为主分类模型，将 ResNet18 作为 baseline。

## 六、失败案例分析部分

### 1. 分类失败案例

分类失败案例建议围绕“细粒度类别相似”展开，而不是只描述某一张图预测错了。

可归纳为以下几类：

- 斗牛犬相关类别容易混淆，例如 American Pit Bull Terrier、Staffordshire Bull Terrier、American Bulldog。
- 长毛猫类别容易混淆，例如 Ragdoll 与 Birman。
- 深色短毛猫类别容易混淆，例如 Russian Blue 与 Bombay。
- 某些图像存在姿态、遮挡、裁剪或背景干扰，使模型难以利用完整身体结构。

### 2. 分割失败案例

分割失败案例建议围绕“边界和前景背景关系”展开。

可归纳为以下几类：

- 宠物毛发边界复杂，预测 mask 难以完全贴合细节。
- 浅色宠物与浅色背景对比不足，前景区域容易漏分或错分。
- 近景裁剪导致上下文不足，模型难以判断完整主体范围。
- trimap 标注本身存在边界模糊，导致模型预测边界与标注边界不完全一致。

### 3. 可写入正文的初级表述

从失败案例看，分类任务的主要错误来源是细粒度品种之间的视觉相似性。部分犬类品种在头部轮廓、体型和毛色上差异较小，部分猫类品种在毛色、纹理和脸部结构上也容易受到拍摄角度与遮挡影响。分割任务的主要困难则集中在复杂边界和低对比区域，尤其是长毛宠物、浅色背景、近景裁剪和标注边界模糊的样本。

## 七、结论与改进方向部分

### 1. 结论建议

结论部分可以压缩为三层：

1. 实验完成情况：完成 37 类宠物品种分类和宠物前景二分类语义分割。
2. 主要结果：EfficientNet-B0 优于 ResNet18；DeepLabV3-MobileNetV3 取得较好的 mIoU 和 Dice。
3. 实验认识：迁移学习适合本任务，但细粒度分类和边界分割仍是难点。

### 2. 改进方向建议

可写三到四点，不宜过多：

- 分类任务可尝试更细致的数据增强、类别重采样或难例挖掘，改善易混淆类别表现。
- 分类模型可尝试注意力机制、多尺度输入或局部区域特征，以强化细粒度差异。
- 分割任务可尝试更高输入分辨率、边界损失或 CRF/形态学后处理，提高边界质量。
- 可进一步探索分割结果辅助分类，例如先裁剪宠物前景再进行品种分类，但作为后续扩展即可。

### 3. 可写入正文的初级表述

综上，本实验完成了 Oxford-IIIT Pet 数据集上的宠物品种分类与宠物前景语义分割。分类任务中，EfficientNet-B0 在测试集上取得 Accuracy 0.899700、Macro-F1 0.898173，整体优于 ResNet18 baseline；分割任务中，DeepLabV3-MobileNetV3 取得 mIoU 0.924921 和 Dice 0.954638，能够较稳定地提取宠物前景。后续改进可从细粒度分类特征增强、困难类别数据增强、分割边界优化和前景辅助分类等方向展开。

## 八、最终写作顺序建议

建议实际写报告时按以下顺序组织：

1. 先放分类结果表，说明 EfficientNet-B0 优于 ResNet18。
2. 再放分类混淆矩阵和失败样例，解释错误来自相似品种。
3. 放分割结果表，说明 DeepLabV3-MobileNetV3 的像素级表现。
4. 放分割预测图，解释好样例与困难样例差异。
5. 放轻量消融表，明确主模型选择依据。
6. 最后用一段结论总结实验成果、局限和改进方向。

这样安排能保证第六节从“结果展示”自然过渡到“结果解释”，最后落到“实验结论”。
