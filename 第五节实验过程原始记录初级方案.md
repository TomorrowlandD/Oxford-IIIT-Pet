# 五、实验过程原始记录初级方案

本文件用于规划实验报告中“五、实验过程原始记录（测试数据、图表、计算等）”部分的填写内容。当前版本是初级方案，重点先确定本节应放哪些原始记录、图表和数据文件，后续可再讨论压缩、扩写或改写为正式 LaTeX 正文。

## 1. 本节写作目标

第五节建议定位为“实验执行过程与原始材料的集中展示”，主要回答：

- 数据是否真实读取、如何划分、样本与类别分布是否可追溯；
- 分类和分割训练过程是否有日志记录；
- 测试指标、预测结果和图表来自哪些文件；
- 后续第六节的结果分析有哪些原始依据。

本节不建议写成完整结论分析。分类模型优劣、分割误差原因、消融实验解释等内容可以只做简短说明，详细讨论放到第六节“实验结果、分析和结论”。

## 2. 建议小节结构

建议第五节采用以下结构：

1. 数据样例与类别分布
2. Mask 转换与预处理检查记录
3. 训练过程记录
4. 模型输出样例
5. 测试数据与计算结果记录
6. 实验记录索引

如果报告篇幅受限，可以合并为四个小节：

1. 数据与预处理原始记录
2. 训练日志与曲线记录
3. 测试结果与可视化记录
4. 实验文件索引

目前更推荐第一种六小节结构，因为它能把“测试数据、图表、计算等”覆盖得更清楚。

## 3. 数据样例与类别分布

### 应填写内容

- 数据集名称：Oxford-IIIT Pet。
- 类别数量：37 类宠物品种。
- 官方 `trainval` 样本数：3680。
- 固定划分后训练集样本数：2944。
- 验证集样本数：736。
- 官方测试集样本数：3669。
- 随机种子：42。
- 展示数据样例图和类别分布图。

### 建议放入图表

| 类型 | 建议标题 | 文件 |
| --- | --- | --- |
| 图 | Oxford-IIIT Pet 数据集样例 | `outputs/figures/fig_dataset_samples.png` |
| 图 | 训练集类别分布 | `outputs/figures/fig_class_distribution.png` |

### 支撑文件

- `outputs/results/data_summary.json`
- `outputs/results/class_distribution.csv`
- `outputs/results/split_indices_seed42.json`

### 写作重点

说明本实验不是临时随机抽样，而是保存了固定划分索引；测试集只用于最终测试，保证测试结果可复现。

## 4. Mask 转换与预处理检查记录

### 应填写内容

- 原始 trimap 像素值为 `1`、`2`、`3`。
- 像素含义：`1` 为宠物前景，`2` 为背景，`3` 为边界。
- 本实验采用二分类前景分割规则：`1 -> 1`，`2 -> 0`，`3 -> 1`。
- 分类输入尺寸为 `224 x 224`。
- 分割输入尺寸为 `320 x 320`。
- mask resize 使用最近邻插值，避免标签值被插值污染。
- 图像与 mask 的空间变换同步执行。

### 建议放入图表

| 类型 | 建议标题 | 文件 |
| --- | --- | --- |
| 图 | Trimap 转二值 mask 样例 | `outputs/figures/fig_mask_samples.png` |
| 图 | 图像预处理检查样例 | `outputs/figures/fig_preprocessing_check.png` |

### 支撑文件

- `outputs/results/trimap_rule_step4.json`
- `outputs/results/mask_conversion_check.json`
- `outputs/results/preprocessing_check.json`
- `outputs/results/dataloader_check.json`

### 写作重点

这一小节用于证明分割标签转换和数据加载没有明显错误，尤其要说明边界像素并入前景的规则，否则后续 mIoU、Dice 的计算口径不清楚。

## 5. 训练过程记录

### 应填写内容

建议用一张表记录三个核心实验：

| 实验编号 | 任务 | 模型 | 输入尺寸 | Epoch | 最佳 Epoch | 训练日志 |
| --- | --- | --- | ---: | ---: | ---: | --- |
| Exp-1 | 分类 | ResNet18 | 224 | 20 | 15 | `outputs/logs/resnet18_cls_log.csv` |
| Exp-2 | 分类 | EfficientNet-B0 | 224 | 25 | 13 | `outputs/logs/efficientnet_b0_cls_log.csv` |
| Exp-3 | 分割 | DeepLabV3-MobileNetV3 | 320 | 30 | 23 | `outputs/logs/deeplabv3_mobilenet_seg_log.csv` |

### 建议放入图表

| 类型 | 建议标题 | 文件 |
| --- | --- | --- |
| 图 | 分类模型训练曲线 | `outputs/figures/fig_cls_training_curve.png` |
| 图 | 分割模型训练曲线 | `outputs/figures/fig_seg_training_curve.png` |

### 支撑文件

- `outputs/logs/resnet18_cls_log.csv`
- `outputs/logs/efficientnet_b0_cls_log.csv`
- `outputs/logs/deeplabv3_mobilenet_seg_log.csv`
- `outputs/results/cls_ablation_val_summary.csv`
- `outputs/results/cls_ablation_test_summary.csv`
- `outputs/results/seg_metrics_deeplabv3_mobilenet.json`

### 写作重点

这里只需要描述训练过程是否正常收敛、最佳 epoch 出现在哪里、曲线是否存在明显异常。不要展开太多模型优劣分析，避免和第六节重复。

## 6. 模型输出样例

### 应填写内容

分类任务建议展示 EfficientNet-B0 的正确分类样例和失败案例；分割任务建议展示预测 mask 与真实 mask 的对比。

### 建议放入图表

| 类型 | 建议标题 | 文件 |
| --- | --- | --- |
| 图 | EfficientNet-B0 分类正确样例 | `outputs/figures/fig_cls_correct_examples.png` |
| 图 | EfficientNet-B0 高置信度分类错误样例 | `outputs/figures/fig_failure_cases.png` |
| 图 | DeepLabV3-MobileNetV3 分割预测对比 | `outputs/figures/fig_seg_predictions.png` |

### 支撑文件

- `outputs/results/cls_examples_efficientnet_b0.json`
- `outputs/results/cls_predictions_efficientnet_b0_test.csv`
- `outputs/results/seg_predictions_deeplabv3_mobilenet_test.csv`
- `outputs/results/seg_visualized_samples.json`
- `outputs/results/seg_failure_analysis_step10.md`

### 写作重点

本小节适合写“图中展示了什么”，不要把失败原因分析写得过长。分类错误和分割困难样例可以留到第六节详细讨论。

## 7. 测试数据与计算结果记录

### 分类测试记录

建议列出 ResNet18 和 EfficientNet-B0 的测试指标，用于证明后续分析基于真实测试输出。

| 模型 | Accuracy | Macro-F1 | Weighted-F1 | Top-5 Accuracy |
| --- | ---: | ---: | ---: | ---: |
| ResNet18 | 0.883074 | 0.881759 | 0.882539 | 0.986645 |
| EfficientNet-B0 | 0.899700 | 0.898173 | 0.898878 | 0.995639 |

支撑文件：

- `outputs/results/cls_metrics_resnet18.json`
- `outputs/results/cls_metrics_efficientnet_b0.json`
- `outputs/results/cls_ablation_test_summary.csv`
- `outputs/results/cls_predictions_resnet18_test.csv`
- `outputs/results/cls_predictions_efficientnet_b0_test.csv`

### 分割测试记录

建议列出 DeepLabV3-MobileNetV3 的测试指标。

| 模型 | Pixel Accuracy | Background IoU | Foreground IoU | mIoU | Dice |
| --- | ---: | ---: | ---: | ---: | ---: |
| DeepLabV3-MobileNetV3 | 0.961981 | 0.936630 | 0.913212 | 0.924921 | 0.954638 |

支撑文件：

- `outputs/results/seg_metrics_deeplabv3_mobilenet.json`
- `outputs/results/seg_summary_deeplabv3_mobilenet_test.csv`
- `outputs/results/seg_predictions_deeplabv3_mobilenet_test.csv`

### 建议放入图表

| 类型 | 建议标题 | 文件 |
| --- | --- | --- |
| 图 | EfficientNet-B0 测试集混淆矩阵 | `outputs/figures/fig_confusion_matrix.png` |
| 图 | ResNet18 测试集混淆矩阵 | `outputs/figures/fig_confusion_matrix_resnet18.png` |

### 写作重点

这一小节中的指标必须来自输出文件，不要手工虚构。可以在正文中说明每个指标的计算对象，例如 Accuracy、Macro-F1 用于 37 类分类，Pixel Accuracy、IoU、mIoU、Dice 用于二值前景分割。

## 8. 实验记录索引

### 应填写内容

建议第五节末尾放一张“主要实验记录文件索引”表，方便老师或读者追溯结果来源。

| 任务 | 记录类型 | 文件路径 |
| --- | --- | --- |
| 数据划分 | 固定划分索引 | `outputs/results/split_indices_seed42.json` |
| 数据统计 | 数据集摘要 | `outputs/results/data_summary.json` |
| ResNet18 分类 | 训练日志 | `outputs/logs/resnet18_cls_log.csv` |
| ResNet18 分类 | 最佳权重 | `outputs/checkpoints/best_cls_resnet18.pth` |
| EfficientNet-B0 分类 | 训练日志 | `outputs/logs/efficientnet_b0_cls_log.csv` |
| EfficientNet-B0 分类 | 最佳权重 | `outputs/checkpoints/best_cls_efficientnet_b0.pth` |
| DeepLabV3-MobileNetV3 分割 | 训练日志 | `outputs/logs/deeplabv3_mobilenet_seg_log.csv` |
| DeepLabV3-MobileNetV3 分割 | 最佳权重 | `outputs/checkpoints/best_seg_deeplabv3_mobilenet.pth` |
| 分类评估 | 测试汇总 | `outputs/results/cls_ablation_test_summary.csv` |
| 分割评估 | 测试汇总 | `outputs/results/seg_summary_deeplabv3_mobilenet_test.csv` |

### 写作重点

索引表不需要展开解释每个文件的全部内容，只要说明它们分别对应训练日志、模型权重、测试指标、预测结果和可视化材料即可。

## 9. 当前建议的正文取舍

建议第五节保留：

- 数据样例图；
- 类别分布图；
- mask 转换样例图；
- 分类训练曲线；
- 分割训练曲线；
- EfficientNet-B0 分类正确样例图；
- DeepLabV3-MobileNetV3 分割预测对比图；
- 实验记录索引表；
- 分类和分割测试指标摘要表。

可选保留：

- ResNet18 混淆矩阵；
- EfficientNet-B0 高置信度错误样例；
- 预处理检查图。

如果版面有限，建议优先删减预处理检查图和 ResNet18 混淆矩阵；主线图表应保留数据样例、训练曲线、主模型输出样例和测试指标表。

## 10. 与第六节的边界

第五节只做原始记录展示和简要说明：

- 记录数据如何划分；
- 记录训练曲线如何变化；
- 记录测试指标是多少；
- 记录图表与文件保存在哪里。

第六节再做结果分析：

- 为什么 EfficientNet-B0 优于 ResNet18；
- 混淆矩阵中哪些类别容易混淆；
- 分割模型在哪些场景表现好或失败；
- 消融实验说明了什么；
- 最终结论和不足。

这样可以避免第五节和第六节重复，也符合“实验过程原始记录”这一标题的要求。

