# 三、所用仪器、材料初级方案

本文件用于规划实验报告中“三、所用仪器、材料（设备名称、型号、规格等或使用软件、数据）”部分的填写内容。当前版本是初级方案，后续可根据老师模板要求继续压缩、扩写或改成正式 LaTeX 表格。

## 1. 本节写作目标

这一节主要回答“实验依托什么硬件、软件和数据完成”。建议不要写成实验方法分析，也不要提前展开训练过程和结果指标；重点应放在：

- 计算设备与运行环境；
- 使用的软件、框架和主要版本；
- 使用的数据集及其用途；
- 实验输出材料的保存位置，便于说明实验可复现。

## 2. 建议小节结构

建议在报告中保留以下三个小节：

1. 实验环境
2. 数据集
3. 主要软件库与输出材料

如果模板要求较简洁，也可以合并为一张“实验仪器、软件与数据”表。

## 3. 实验环境拟填写内容

可以用表格填写，避免正文过长。

| 项目 | 内容 |
| --- | --- |
| 操作系统 | Windows 环境 |
| 终端环境 | PowerShell 5.1.26100.4768 |
| Python 环境 | conda 环境 `unet` |
| Python 路径 | `D:\Anaconda3\envs\unet\python.exe` |
| Python 版本 | Python 3.10.20 |
| 深度学习框架 | PyTorch 2.11.0+cu126 |
| 视觉工具库 | TorchVision 0.26.0+cu126 |
| CUDA | CUDA 12.6，可用 |
| GPU | NVIDIA GeForce RTX 4060 Laptop GPU |
| 报告编译工具 | XeLaTeX |

正文可写为：

> 本实验在 Windows 系统下完成，统一使用 conda 环境 `unet` 运行训练、评估和可视化脚本。深度学习框架采用 PyTorch 与 TorchVision，训练过程使用 CUDA 加速，主要计算设备为 NVIDIA GeForce RTX 4060 Laptop GPU。实验报告使用 XeLaTeX 编译。

## 4. 数据集拟填写内容

数据集建议单独说明，因为它既是“材料”，也是分类和分割两个任务的共同基础。

| 项目 | 内容 |
| --- | --- |
| 数据集名称 | Oxford-IIIT Pet |
| 数据目录 | `data/step6_data/oxford-iiit-pet` |
| 类别数量 | 37 类宠物品种 |
| 分类标注 | 品种类别标签 |
| 分割标注 | trimap 标注 |
| 分割任务设置 | 将 trimap 转换为背景/宠物前景二分类 mask |
| 官方 trainval 样本数 | 3680 |
| 训练集样本数 | 2944 |
| 验证集样本数 | 736 |
| 测试集样本数 | 3669 |
| 随机种子 | 42 |

正文可写为：

> 本实验使用 Oxford-IIIT Pet 数据集作为实验材料。该数据集包含 37 类宠物品种图像，并提供分类标签和 trimap 分割标注。分类任务使用品种类别标签完成 37 类识别；分割任务将原始 trimap 标注转换为背景与宠物前景两类 mask。实验中将官方 `trainval` 按固定随机种子 42 划分为训练集和验证集，官方 `test` 划分仅用于最终测试评估。

## 5. 主要软件库拟填写内容

这一部分建议列出关键库及用途，不需要把所有依赖都写进去。

| 软件/库 | 版本 | 主要用途 |
| --- | --- | --- |
| PyTorch | 2.11.0+cu126 | 模型构建、训练、损失计算、GPU 加速 |
| TorchVision | 0.26.0+cu126 | Oxford-IIIT Pet 数据集读取、预训练模型加载、图像变换 |
| NumPy | 1.23.5 | 数值计算、随机种子和数组处理 |
| pandas | 2.3.3 | 日志、预测结果和指标表格保存 |
| scikit-learn | 1.7.2 | 分类指标、Macro-F1、混淆矩阵等计算 |
| Matplotlib | 3.6.2 | 训练曲线、类别分布、混淆矩阵和分割结果可视化 |
| XeLaTeX | 待确认具体版本 | 实验报告编译 |

正文可写为：

> 主要程序实现基于 PyTorch 和 TorchVision。PyTorch 用于模型训练和张量计算，TorchVision 用于加载预训练模型和 Oxford-IIIT Pet 数据集。实验结果统计和可视化分别使用 pandas、scikit-learn、NumPy 和 Matplotlib 完成。

## 6. 模型与实验材料是否放在本节

建议可以简要提到模型名称，但不要在本节展开模型原理。模型原理应放在“实验原理”或“实验方法、步骤”部分。

可以在本节中简写：

- 分类 baseline：ResNet18，加载 ImageNet 预训练权重；
- 分类主模型：EfficientNet-B0，加载 ImageNet 预训练权重；
- 分割主模型：DeepLabV3-MobileNetV3，加载预训练 backbone，并替换为二分类输出头。

如果第三节篇幅有限，模型名称可以不单独成表，只在“主要软件库与模型资源”中用一两句话说明。

## 7. 输出材料拟填写内容

这一部分有助于体现实验是完整执行的，也便于后续报告引用。

| 输出目录 | 内容 |
| --- | --- |
| `outputs/checkpoints/` | 保存分类和分割模型最佳权重 |
| `outputs/logs/` | 保存训练日志 CSV |
| `outputs/figures/` | 保存数据样例、训练曲线、混淆矩阵、分割可视化等图片 |
| `outputs/results/` | 保存指标 JSON/CSV、逐样本预测结果和数据划分记录 |

正文可写为：

> 实验过程中产生的模型权重、训练日志、评估指标和可视化图片均保存在 `outputs/` 目录下。其中 `outputs/checkpoints/` 保存模型权重，`outputs/logs/` 保存训练日志，`outputs/figures/` 保存报告插图，`outputs/results/` 保存指标文件和预测结果，便于报告撰写与结果复核。

## 8. 推荐最终写法方向

当前报告第三节可以采用“短正文 + 两到三张表”的形式：

1. 第一段概述实验环境和 GPU；
2. 表 1 列出硬件、Python、PyTorch、CUDA 等环境；
3. 第二段说明 Oxford-IIIT Pet 数据集；
4. 表 2 列出数据划分；
5. 最后一小节列出主要软件库和用途。

这样写的优点是信息完整、结构清晰，并且不会和后文“实验方法、步骤”重复。

## 9. 后续讨论点

后续可以继续确认以下问题：

- 是否需要写出 CPU、内存、显存等更完整的硬件规格；
- 是否需要补充 XeLaTeX 的具体版本；
- 是否把模型名称放在第三节，还是只放到实验原理和实验方法部分；
- 是否将本方案直接改写为 `main.tex` 中的正式内容。
