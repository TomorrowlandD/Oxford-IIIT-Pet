## 核心方案

你的项目可以正式定为：

> **基于迁移学习的 Oxford-IIIT Pet 宠物品种分类与前景语义分割综合实践**

建议采用 **同一数据集、两个任务、两条模型分支** 的方案：

| 模块       | 任务         | 推荐模型                          | 输出        | 报告价值                     |
| -------- | ---------- | ----------------------------- | --------- | ------------------------ |
| **任务一**  | 37 类宠物品种分类 | ResNet18 / EfficientNet-B0 微调 | 宠物品种类别    | 分类任务、迁移学习、混淆矩阵、错误分析      |
| **任务二**  | 宠物前景语义分割   | DeepLabV3-MobileNetV3 | 宠物前景 mask | 像素级预测、mIoU、Dice、mask 可视化 |
| **扩展分析** | 轻量消融实验       | ResNet18 vs EfficientNet-B0，或有无数据增强 | 指标表格      | 让报告不像单纯跑代码               |

Oxford-IIIT Pet 数据集本身包含 **37 类宠物品种**，每类约 200 张图片，并提供 **品种标签、头部 ROI 和像素级 trimap 分割标注**，很适合同时做分类和分割任务。([牛津大学机器人研究][1]) TorchVision 也直接支持该数据集，并且 `target_types` 可以选择 `category`、`binary-category` 或 `segmentation`，适合用 PyTorch 快速构建实验流程。([PyTorch Docs][2])

---

## 一、项目最终定位

### 1. 建议题目

可以在报告封面或正文中使用下面这个题目：

> **基于迁移学习的宠物图像品种分类与前景语义分割研究**

或者更贴近课程实践：

> **Oxford-IIIT Pet 数据集上的图像分类与语义分割综合实践**

### 2. 项目难度定位

这个项目属于 **中等难度**：

* 比单纯图像分类更丰富；
* 不需要自己采集和标注数据；
* 不需要训练大模型；
* RTX 4060 8GB 可以完成；
* 实验报告中可以自然包含：

  * **数据预处理**
  * **模型微调**
  * **分类评估**
  * **分割评估**
  * **训练曲线**
  * **混淆矩阵**
  * **mask 可视化**
  * **失败案例分析**

### 3. 建议完成范围

建议把工作量收敛到 **标准最终版**。这个版本已经足够支撑课程期末报告，不需要一开始就加入 U-Net、Grad-CAM 或“分割辅助分类”等扩展内容。

| 版本        | 内容                                                       | 是否必须     |
| --------- | -------------------------------------------------------- | -------- |
| **标准最终版** | EfficientNet-B0 分类 + ResNet18 baseline + DeepLabV3-MobileNetV3 分割 | **必须完成** |
| **轻量消融** | ResNet18 与 EfficientNet-B0 对比，或有无数据增强对比                       | **推荐完成一个即可** |
| **可选扩展 1** | Grad-CAM 分类可解释性可视化                                    | 时间充裕再做       |
| **可选扩展 2** | U-Net 与 DeepLabV3 分割对比                                | 时间充裕再做       |
| **可选扩展 3** | 将分割结果用于裁剪前景，再做分类对比                                       | 不建议作为本次主线  |

最终报告的核心目标不是堆模型数量，而是把 **数据处理、模型训练、指标评估、可视化和结果分析** 写完整。只要标准最终版跑通，并补充一个小型对比实验，项目对学生课程作业来说已经足够。

---

## 二、推荐实验总体设计

### 1. 数据集划分

建议使用 TorchVision 提供的官方划分方式：

| 数据部分       | 用途            |
| ---------- | ------------- |
| `trainval` | 再划分为训练集和验证集   |
| `test`     | 最终测试，只在最后评估一次 |

建议比例：

| 子集  | 来源               | 用途        |
| --- | ---------------- | --------- |
| 训练集 | `trainval` 的 80% | 模型训练      |
| 验证集 | `trainval` 的 20% | 调参、保存最佳模型 |
| 测试集 | 官方 `test`        | 最终结果评估    |

在报告中不要只写“我划分了数据集”，最好写清楚：

> 本实验采用固定随机种子对 `trainval` 部分进行 8:2 划分，保证实验可复现。测试集不参与训练和模型选择，只用于最终性能评估。

---

### 2. 两个任务的设计

#### 任务一：宠物品种分类

输入：

* 宠物 RGB 图像

输出：

* 37 类宠物品种标签

推荐模型：

| 模型                  | 用途                    | 推荐程度 |
| ------------------- | --------------------- | ---- |
| **ResNet18**        | baseline，速度快，显存小      | 高    |
| **EfficientNet-B0** | 主模型，效果通常比 ResNet18 更好 | 高    |
| ResNet50            | 可选，但 8GB 显存下训练更慢      | 中    |

TorchVision 提供了 ResNet18 和 EfficientNet-B0 的预训练权重接口，例如 `ResNet18_Weights.DEFAULT` 和 `EfficientNet_B0_Weights.DEFAULT`，适合用于迁移学习。([PyTorch Docs][3]) PyTorch 官方迁移学习教程也说明了使用预训练 CNN 进行图像分类微调的基本流程。([PyTorch Docs][4])

---

#### 任务二：宠物前景语义分割

输入：

* 宠物 RGB 图像

输出：

* 每个像素属于 **背景** 还是 **宠物前景**

推荐先做 **二分类前景分割**：

| 原始 trimap 区域     | 推荐处理    |
| ---------------- | ------- |
| pet              | 前景，记为 1 |
| background       | 背景，记为 0 |
| border / outline | 忽略或并入前景 |

Oxford-IIIT Pet 的 trimap mask 通常包含 `1`、`2`、`3` 三种像素值，分别对应 pet、background、border；实际实现时建议先用 `np.unique(mask)` 验证。([Albumentations][5])

推荐分割模型：

| 模型                        | 用途                      | 推荐程度 |
| ------------------------- | ----------------------- | ---- |
| **DeepLabV3-MobileNetV3** | 主模型，较轻量，适合 8GB 显存       | 高    |
| U-Net                     | 可选扩展，结构清晰，但会增加实现和调参工作量 | 低    |
| DeepLabV3-ResNet50        | 可选扩展，效果可能更好，但显存压力更大          | 低    |

TorchVision 提供了 `deeplabv3_mobilenet_v3_large`，它构建的是以 **MobileNetV3-Large** 为 backbone 的 DeepLabV3 分割模型，适合做轻量化语义分割微调。([PyTorch Docs][6])

---

## 三、建议源码目录结构

你的项目源文件夹可以整理成下面这样：

```text
pet_cv_project/
├── data/
│   └── oxford-iiit-pet/              # 数据集自动下载或手动放置
├── report/
│   ├── main.tex                      # 你的 LaTeX 实验报告模板
│   ├── figures/                      # 报告中使用的图片
│   └── tables/                       # 可选：保存 LaTeX 表格
├── src/
│   ├── config.py                     # 全局配置
│   ├── datasets.py                   # 数据集读取、mask 转换、数据增强
│   ├── models_cls.py                 # 分类模型
│   ├── models_seg.py                 # 分割模型
│   ├── train_cls.py                  # 分类训练
│   ├── train_seg.py                  # 分割训练
│   ├── eval_cls.py                   # 分类评估
│   ├── eval_seg.py                   # 分割评估
│   ├── visualize.py                  # 可视化结果生成
│   └── utils.py                      # 随机种子、日志、指标等工具函数
├── outputs/
│   ├── checkpoints/
│   │   ├── best_cls.pth
│   │   └── best_seg.pth
│   ├── logs/
│   │   ├── cls_log.csv
│   │   └── seg_log.csv
│   ├── figures/
│   │   ├── dataset_samples.png
│   │   ├── mask_samples.png
│   │   ├── cls_curves.png
│   │   ├── confusion_matrix.png
│   │   ├── seg_curves.png
│   │   └── seg_visualization.png
│   └── results/
│       ├── cls_metrics.json
│       └── seg_metrics.json
├── requirements.txt
└── README.md
```

---

## 四、建议实验配置

### 1. 硬件与环境

报告中可以写：

| 项目     | 建议内容                                             |
| ------ | ------------------------------------------------ |
| 操作系统   | Windows 11 或 Ubuntu 22.04                        |
| 编程语言   | Python 3.10 / Python 3.11                        |
| 深度学习框架 | PyTorch、TorchVision                              |
| GPU    | NVIDIA GeForce RTX 4060 8GB                      |
| CUDA   | 按本机 PyTorch 环境填写                                 |
| 主要库    | NumPy、OpenCV、Matplotlib、scikit-learn、tqdm、Pandas |
| 编辑器    | VS Code / PyCharm / Jupyter Notebook             |

### 2. 分类任务训练配置

| 参数              | 建议值                                     |
| --------------- | --------------------------------------- |
| 输入尺寸            | $224 \times 224$                        |
| batch size      | 16 或 32                                 |
| epoch           | 20–30                                   |
| optimizer       | AdamW                                   |
| learning rate   | $1 \times 10^{-4}$ 或 $3 \times 10^{-4}$ |
| loss            | Cross Entropy Loss                      |
| scheduler       | CosineAnnealingLR 或 StepLR              |
| mixed precision | 开启 AMP                                  |
| 保存策略            | 验证集 Macro-F1 或 Accuracy 最优时保存           |

### 3. 分割任务训练配置

| 参数              | 建议值                                 |
| --------------- | ----------------------------------- |
| 输入尺寸            | $256 \times 256$ 或 $320 \times 320$ |
| batch size      | 2、4 或 8                             |
| epoch           | 25–30                               |
| optimizer       | AdamW                               |
| learning rate   | $1 \times 10^{-4}$                  |
| loss            | Cross Entropy Loss，可加 Dice Loss     |
| 输出类别            | 二分类：背景 / 宠物前景                       |
| mixed precision | 开启 AMP                              |
| 保存策略            | 验证集 mIoU 或 Dice 最优时保存               |

对于 RTX 4060 8GB，建议：

* 分类从 `batch_size=32` 开始；
* 分割从 `batch_size=4` 开始；
* 如果显存不足，优先减小 batch size；
* 分割输入尺寸不要一开始就用 $512 \times 512$；
* 标准版建议直接使用 $256 \times 256$ 或 $320 \times 320$ 中的一个尺寸，不必强制做尺寸对比。

---

## 五、实验报告各部分具体写法

下面按你的 LaTeX 模板逐节设计。

---

## 一、实验目的及内容

### 1. 实验目的

这一节建议写成下面几个点：

```latex
本实验以 Oxford-IIIT Pet 数据集为研究对象，围绕图像分类和语义分割两个典型计算机视觉任务开展综合实践。实验旨在掌握图像数据预处理、卷积神经网络迁移学习、模型微调、图像语义分割、模型评估与结果可视化等基本方法。
```

可以展开为：

1. **掌握图像分类任务的完整流程**
   包括数据读取、图像增强、预训练模型微调、分类指标计算和混淆矩阵分析。

2. **掌握语义分割任务的基本方法**
   使用 trimap 分割标注训练像素级预测模型，实现宠物前景区域提取。

3. **理解迁移学习在小规模数据集上的作用**
   比较预训练模型与普通训练方式的差异。

4. **掌握模型评估与可视化方法**
   包括 Accuracy、Macro-F1、mIoU、Dice、训练曲线、分割结果叠加图等。

### 2. 实验内容

建议写：

```latex
本实验主要完成以下内容：
```

然后列出：

```latex
\begin{itemize}
  \item 使用 Oxford-IIIT Pet 数据集完成宠物图像读取、标签解析和分割 mask 预处理；
  \item 构建基于 ResNet18 或 EfficientNet-B0 的宠物品种分类模型，实现 37 类宠物品种识别；
  \item 构建基于 DeepLabV3-MobileNetV3 的宠物前景语义分割模型，实现背景与宠物区域的像素级划分；
  \item 对分类模型计算 Accuracy、Macro-F1、混淆矩阵等指标；
  \item 对分割模型计算 mIoU、Dice、Pixel Accuracy 等指标；
  \item 可视化分类预测结果、错误分类样例、真实 mask 与预测 mask 对比图；
  \item 分析实验结果，总结模型优点、不足与改进方向。
\end{itemize}
```

---

## 二、实验原理及技术路线

### 1. 概述

这一节要解释为什么这个项目是一个综合实践。

建议写：

```latex
本实验选取 Oxford-IIIT Pet 数据集作为实验对象。该数据集包含多种猫和狗的宠物图像，并提供宠物品种类别标签和像素级 trimap 分割标注，因此可以同时用于图像分类和语义分割任务。实验整体分为两个子任务：第一部分利用预训练卷积神经网络完成宠物品种分类；第二部分利用语义分割模型完成宠物前景区域提取。
```

可以补充说明：

* 图像分类属于 **图像级预测**；
* 语义分割属于 **像素级预测**；
* 二者结合可以体现计算机视觉中不同层次的理解能力。

---

### 2. 实验原理

建议分成几个小点写。

#### 2.1 图像分类原理

图像分类模型的目标是学习一个映射：

$$
f_{\theta}(x) \rightarrow y
$$

其中 $x$ 为输入图像，$y$ 为宠物品种类别，$\theta$ 为模型参数。

分类任务使用交叉熵损失：

$$
\mathcal{L}_{\mathrm{cls}} = - \sum_{c=1}^{C} y_c \log(\hat{y}_c)
$$

其中：

* $C=37$；
* $y_c$ 是真实类别的 one-hot 标签；
* $\hat{y}_c$ 是模型预测属于第 $c$ 类的概率。

你可以解释：

> 本实验使用 ImageNet 预训练模型作为特征提取器，将最后的分类层替换为 37 类输出层，并在 Oxford-IIIT Pet 数据集上进行微调。这样可以利用预训练模型已经学习到的边缘、纹理、形状等通用视觉特征，从而提高小规模数据集上的训练效果。

---

#### 2.2 迁移学习原理

迁移学习的核心思想是：

> 使用大规模数据集上训练好的模型参数作为初始化，再在目标任务上进行微调。

你可以写：

```latex
由于 Oxford-IIIT Pet 数据集规模相对有限，如果从零开始训练深层卷积神经网络，容易出现收敛慢和过拟合问题。因此，本实验采用迁移学习方法，使用预训练模型初始化网络主体，仅替换最后的任务输出层，然后在宠物图像数据集上微调模型参数。
```

建议在报告中区分两种策略：

| 策略          | 说明                      |
| ----------- | ----------------------- |
| **冻结特征提取层** | 只训练最后分类层，训练速度快          |
| **全模型微调**   | 解冻全部或部分 backbone，效果可能更好 |
| **对比实验**    | 比较冻结与微调的结果差异            |

---

#### 2.3 语义分割原理

语义分割是对图像中每个像素进行分类。

模型学习的映射为：

$$
g_{\phi}(x) \rightarrow M
$$

其中：

* $x$ 为输入图像；
* $M$ 为像素级分割 mask；
* $\phi$ 为分割模型参数。

二分类前景分割中，每个像素属于：

| 类别编号 | 含义   |
| ---- | ---- |
| 0    | 背景   |
| 1    | 宠物前景 |

对于分割任务，可以使用 Cross Entropy Loss，也可以加入 Dice Loss：

$$
\mathcal{L}_{\mathrm{seg}} = \mathcal{L}_{\mathrm{CE}} + \lambda \mathcal{L}_{\mathrm{Dice}}
$$

Dice 系数定义为：

$$
\mathrm{Dice} = \frac{2TP}{2TP + FP + FN}
$$

IoU 定义为：

$$
\mathrm{IoU} = \frac{TP}{TP + FP + FN}
$$

多类别平均 IoU 为：

$$
\mathrm{mIoU} = \frac{1}{C} \sum_{c=1}^{C} \mathrm{IoU}_c
$$

---

#### 2.4 DeepLabV3 原理

这里不需要写得太深，但要体现你理解了模型。

可以写：

```latex
DeepLabV3 是一种经典语义分割网络，其核心思想是利用空洞卷积扩大感受野，并通过多尺度上下文信息提升像素级分类效果。本实验使用较轻量的 MobileNetV3 作为 backbone，从而在 RTX 4060 8GB 显存条件下实现较高效率的分割模型训练。
```

---

### 3. 技术路线

建议报告中放一个流程图。文字版本可以写成：

```text
Oxford-IIIT Pet 数据集
        │
        ├── 图像 + 品种标签
        │       │
        │       ├── 图像 resize、归一化、数据增强
        │       ├── ResNet18 / EfficientNet-B0 微调
        │       ├── Accuracy、Macro-F1、混淆矩阵评估
        │       └── 分类结果与错误样例可视化
        │
        └── 图像 + trimap mask
                │
                ├── mask 标签转换、resize、同步增强
                ├── DeepLabV3-MobileNetV3 分割训练
                ├── mIoU、Dice、Pixel Accuracy 评估
                └── 原图、真实 mask、预测 mask 叠加可视化
```

也可以在 LaTeX 里写：

```latex
本实验技术路线主要包括数据准备、图像预处理、分类模型训练、分割模型训练、模型评估与结果可视化五个阶段。
```

然后配表：

| 阶段    | 主要操作                                | 输出     |
| ----- | ----------------------------------- | ------ |
| 数据准备  | 下载 Oxford-IIIT Pet，读取图像、类别标签、trimap | 数据集对象  |
| 数据预处理 | resize、归一化、数据增强、mask 转换             | 可训练样本  |
| 分类训练  | 微调 EfficientNet-B0 / ResNet18       | 分类模型权重 |
| 分割训练  | 微调 DeepLabV3-MobileNetV3            | 分割模型权重 |
| 评估分析  | 计算指标、绘制图表、分析失败案例                    | 实验结果   |

---

## 三、所用仪器、材料

这一节可以直接写成 LaTeX：

```latex
\begin{itemize}
  \item 操作系统：Windows 11 / Ubuntu 22.04
  \item GPU：NVIDIA GeForce RTX 4060 8GB
  \item 编程语言：Python 3.10
  \item 深度学习框架：PyTorch、TorchVision
  \item 开发环境：VS Code / PyCharm
  \item 使用库：NumPy、OpenCV、Matplotlib、Pandas、scikit-learn、tqdm
  \item 数据集：Oxford-IIIT Pet Dataset
  \item 分类模型：ResNet18、EfficientNet-B0
  \item 分割模型：DeepLabV3-MobileNetV3
\end{itemize}
```

如果你实际只用了 EfficientNet-B0 和 DeepLabV3-MobileNetV3，就不要写 U-Net、ResNet50、SegFormer 等没有运行过的模型。

---

## 四、实验方法、步骤

这一节最重要。老师已经提示 **不能只有代码，应该有代码解释或解读**，所以建议你按“步骤 + 关键代码 + 解释”的形式写。

---

### 步骤 1：数据集读取

报告中可以放关键代码：

```python
from torchvision.datasets import OxfordIIITPet

dataset = OxfordIIITPet(
    root="data",
    split="trainval",
    target_types=["category", "segmentation"],
    download=True
)
```

然后写解释：

> 上述代码使用 TorchVision 读取 Oxford-IIIT Pet 数据集。`split="trainval"` 表示读取官方训练验证部分，`target_types=["category", "segmentation"]` 表示同时读取宠物品种标签和分割 trimap 标注。这样可以在同一份数据上同时完成图像分类和语义分割任务。

---

### 步骤 2：图像预处理

分类任务建议：

```python
train_transform = transforms.Compose([
    transforms.Resize((224, 224)),
    transforms.RandomHorizontalFlip(),
    transforms.ColorJitter(brightness=0.2, contrast=0.2, saturation=0.2),
    transforms.ToTensor(),
    transforms.Normalize(mean=[0.485, 0.456, 0.406],
                         std=[0.229, 0.224, 0.225])
])
```

报告解释：

> 分类模型输入尺寸统一调整为 $224 \times 224$。训练阶段加入随机水平翻转和颜色扰动，用于增强模型对姿态、光照变化的鲁棒性。归一化参数采用 ImageNet 常用均值和标准差，以匹配预训练模型的输入分布。

分割任务需要强调：

> 对图像和 mask 进行 resize 时，图像可以使用双线性插值，而 mask 必须使用最近邻插值，否则会产生非法类别值。

---

### 步骤 3：mask 标签转换

建议你的标准版做 **二分类前景分割**。

关键代码可以写：

```python
import numpy as np

def convert_trimap_to_binary(mask):
    mask = np.array(mask)

    binary_mask = np.zeros_like(mask, dtype=np.uint8)

    # Oxford-IIIT Pet trimap:
    # 1: pet, 2: background, 3: border
    binary_mask[mask == 1] = 1
    binary_mask[mask == 2] = 0

    # border 可以并入前景，也可以忽略
    binary_mask[mask == 3] = 1

    return binary_mask
```

报告解释：

> 原始 trimap 中包含宠物区域、背景区域和边界区域。为了降低任务难度并突出前景分割目标，本实验将宠物区域和边界区域合并为前景，将背景区域作为背景，从而构造二分类语义分割任务。

如果你想更严谨，可以采用 **忽略边界区域** 的版本：

```python
def convert_trimap_with_ignore(mask):
    mask = np.array(mask)

    new_mask = np.zeros_like(mask, dtype=np.uint8)
    new_mask[mask == 2] = 0      # background
    new_mask[mask == 1] = 1      # pet
    new_mask[mask == 3] = 255    # border ignored

    return new_mask
```

对应解释：

> 边界区域通常存在一定模糊性，因此也可以将其设置为 `ignore_index=255`，训练损失和指标计算时不参与统计。本实验可根据实际效果选择是否忽略边界。

我建议你 **先用并入前景版本**，更容易得到完整、好看的可视化结果。

---

### 步骤 4：分类模型构建

推荐主模型使用 EfficientNet-B0。

关键代码：

```python
import torch.nn as nn
from torchvision.models import efficientnet_b0, EfficientNet_B0_Weights

num_classes = 37

model = efficientnet_b0(weights=EfficientNet_B0_Weights.DEFAULT)
in_features = model.classifier[1].in_features
model.classifier[1] = nn.Linear(in_features, num_classes)
```

报告解释：

> EfficientNet-B0 原始输出层对应 ImageNet 类别数，不能直接用于宠物品种分类。因此，本实验保留其预训练特征提取部分，将最后的全连接分类层替换为 37 类输出层，使模型能够输出 Oxford-IIIT Pet 数据集中的宠物品种类别。

如果你想做 baseline，再加入 ResNet18：

```python
from torchvision.models import resnet18, ResNet18_Weights

model = resnet18(weights=ResNet18_Weights.DEFAULT)
model.fc = nn.Linear(model.fc.in_features, 37)
```

报告解释：

> ResNet18 参数量较小、训练速度较快，适合作为分类任务 baseline。通过与 EfficientNet-B0 对比，可以分析不同 CNN backbone 对宠物品种分类效果的影响。

---

### 步骤 5：分类模型训练

分类损失函数：

```python
criterion = nn.CrossEntropyLoss()
```

优化器：

```python
optimizer = torch.optim.AdamW(model.parameters(), lr=1e-4, weight_decay=1e-4)
```

报告解释：

> 分类任务采用交叉熵损失函数，该损失适用于多分类问题。优化器采用 AdamW，相比普通 Adam 增加了权重衰减机制，有助于缓解过拟合。

训练时需要保存：

* train loss；
* val loss；
* val accuracy；
* val macro-F1；
* best checkpoint。

---

### 步骤 6：分割模型构建

推荐使用 DeepLabV3-MobileNetV3。

关键代码示意：

```python
import torch.nn as nn
from torchvision.models.segmentation import deeplabv3_mobilenet_v3_large
from torchvision.models import MobileNet_V3_Large_Weights

num_seg_classes = 2

model = deeplabv3_mobilenet_v3_large(
    weights=None,
    weights_backbone=MobileNet_V3_Large_Weights.DEFAULT,
    num_classes=num_seg_classes
)
```

报告解释：

> 本实验使用 DeepLabV3-MobileNetV3 作为语义分割模型。MobileNetV3 作为 backbone 能够降低计算量和显存占用，适合在 RTX 4060 8GB 显卡上训练。模型输出通道数设置为 2，分别表示背景和宠物前景。

如果你的 TorchVision 版本对 `num_classes` 支持不一致，可以改为手动替换分类头。实际报告中写你最终采用的代码即可。

---

### 步骤 7：分割模型训练

分割损失：

```python
criterion = nn.CrossEntropyLoss()
```

如果使用忽略边界：

```python
criterion = nn.CrossEntropyLoss(ignore_index=255)
```

报告解释：

> 分割任务本质上是对每个像素进行分类，因此同样可以使用交叉熵损失。与图像分类不同，分割任务的损失是在所有像素位置上计算得到的。

训练时保存：

* train loss；
* val loss；
* val mIoU；
* val Dice；
* best checkpoint。

---

### 步骤 8：模型评估

分类任务指标：

| 指标               | 含义                       |
| ---------------- | ------------------------ |
| Accuracy         | 整体分类正确率                  |
| Macro-F1         | 每个类别 F1 的平均值，适合类别数量较多的任务 |
| Confusion Matrix | 分析哪些品种容易混淆               |
| Top-5 Accuracy   | 可选，判断真实类别是否在预测前 5 类中     |

分割任务指标：

| 指标             | 含义                       |
| -------------- | ------------------------ |
| Pixel Accuracy | 像素级分类准确率                 |
| IoU            | 预测区域与真实区域的交并比            |
| mIoU           | 多类别 IoU 平均值              |
| Dice           | 衡量预测 mask 与真实 mask 的重合程度 |

---

### 步骤 9：可视化

你至少应该生成下面这些图：

| 图名                       | 内容                     | 放在报告位置     |
| ------------------------ | ---------------------- | ---------- |
| `dataset_samples.png`    | 数据集原图样例                | 五、实验过程原始记录 |
| `mask_samples.png`       | 原图 + trimap mask       | 五、实验过程原始记录 |
| `class_distribution.png` | 类别数量分布                 | 五、实验过程原始记录 |
| `cls_curves.png`         | 分类 loss / accuracy 曲线  | 五或六        |
| `confusion_matrix.png`   | 分类混淆矩阵                 | 六          |
| `seg_curves.png`         | 分割 loss / mIoU 曲线      | 五或六        |
| `seg_visualization.png`  | 原图、真实 mask、预测 mask、叠加图 | 六          |
| `failure_cases.png`      | 分类或分割失败案例              | 六          |

分割结果建议排版为：

| 原图 | 真实 mask | 预测 mask | 叠加结果 |
| -- | ------- | ------- | ---- |

---

## 五、实验过程原始记录

这一节不要写成“流水账”，而是放 **实验过程中产生的原始记录、图表、日志和样例**。

建议包含以下内容。

### 1. 数据集样例

放几张原图，并说明：

```latex
图中可以看出，宠物图像存在姿态、尺度、背景和光照变化，这增加了分类和分割任务的难度。
```

### 2. mask 样例

展示：

* 原图；
* 原始 trimap；
* 转换后的二值 mask。

说明：

```latex
原始 trimap 标注中包含宠物、背景和边界区域。本实验将其转换为前景/背景二分类 mask，用于训练语义分割模型。
```

### 3. 训练日志截图

可以放终端截图或 TensorBoard / CSV 绘图结果。

建议记录：

| 实验编号  | 任务 | 模型                    | 输入尺寸 | batch size | epoch |
| ----- | -- | --------------------- | ---- | ---------- | ----- |
| Exp-1 | 分类 | ResNet18              | 224  | 32         | 20    |
| Exp-2 | 分类 | EfficientNet-B0       | 224  | 32         | 25    |
| Exp-3 | 分割 | DeepLabV3-MobileNetV3 | 320  | 4          | 30    |

### 4. 训练曲线

分类曲线：

* train loss；
* val loss；
* val accuracy；
* val macro-F1。

分割曲线：

* train loss；
* val loss；
* val mIoU；
* val Dice。

### 5. 模型输出样例

分类输出样例：

| 图像 | 真实类别 | 预测类别 | 置信度 | 是否正确 |
| -- | ---- | ---- | --- | ---- |

分割输出样例：

| 图像 | 真实 mask | 预测 mask | Dice | IoU |
| -- | ------- | ------- | ---- | --- |

---

## 六、实验结果、分析和结论

这一节建议写成 **结果表格 + 分析 + 结论 + 改进方向**。

---

### 1. 分类结果表格

训练完成后填入真实结果，不要虚构。

```latex
\begin{table}[H]
\centering
\caption{宠物品种分类实验结果}
\begin{tabular}{ccccc}
\toprule
模型 & 是否预训练 & 数据增强 & Accuracy & Macro-F1 \\
\midrule
ResNet18 & 是 & 是 & 待填写 & 待填写 \\
EfficientNet-B0 & 是 & 是 & 待填写 & 待填写 \\
\bottomrule
\end{tabular}
\end{table}
```

分析方向：

* EfficientNet-B0 是否优于 ResNet18；
* 数据增强是否提升验证集表现；
* 哪些品种容易混淆；
* 错误分类是否集中在相似品种上。

可以写：

```latex
从混淆矩阵可以看出，模型主要在外观相似的猫狗品种之间发生混淆。例如某些毛色、体型较为接近的品种，其局部纹理和整体轮廓差异较小，因此模型容易给出错误预测。
```

---

### 2. 分割结果表格

```latex
\begin{table}[H]
\centering
\caption{宠物前景分割实验结果}
\begin{tabular}{ccccc}
\toprule
模型 & 输入尺寸 & Pixel Accuracy & mIoU & Dice \\
\midrule
DeepLabV3-MobileNetV3 & 320$\times$320 & 待填写 & 待填写 & 待填写 \\
\bottomrule
\end{tabular}
\end{table}
```

分析方向：

* 分割模型对主体明显、背景简单的图片效果较好；
* 对毛发边缘、遮挡、背景复杂图片效果下降；
* 预测 mask 可能在边界处不够精细；
* 如果宠物和背景颜色相近，分割容易误判。

可以写：

```latex
从可视化结果可以看出，模型能够较好地定位宠物主体区域，对大部分图像能够生成连续的前景 mask。但在宠物毛发边缘、遮挡区域以及背景颜色与宠物颜色接近的情况下，预测结果容易出现边界不准确或局部漏分割现象。
```

---

### 3. 消融实验建议

为了让报告更完整，建议 **只做一个轻量消融实验**。优先选择已经自然包含在主线里的对比，避免额外增加训练成本。

#### 首选消融：ResNet18 vs EfficientNet-B0

这个消融最推荐，因为标准主线本来就包含 ResNet18 baseline 和 EfficientNet-B0 主模型，不需要额外设计新任务。

| 模型              | 参数特点       | Accuracy | Macro-F1 |
| --------------- | ---------- | -------- | -------- |
| ResNet18        | 结构简单，训练快   | 待填写      | 待填写      |
| EfficientNet-B0 | 更高效的复合缩放结构 | 待填写      | 待填写      |

分析方向：

> ResNet18 参数量较小、训练速度较快，适合作为 baseline；EfficientNet-B0 通过更高效的网络结构提升特征表达能力。通过比较两者在测试集上的 Accuracy 和 Macro-F1，可以分析不同 backbone 对宠物品种分类任务的影响。

#### 备选消融：有无数据增强

如果 ResNet18 和 EfficientNet-B0 的训练结果差异不明显，或者你希望突出数据预处理的作用，可以改做有无数据增强对比。

| 实验           | 数据增强 | Accuracy | Macro-F1 |
| ------------ | ---- | -------- | -------- |
| Baseline     | 无    | 待填写      | 待填写      |
| Augmentation | 有    | 待填写      | 待填写      |

分析：

> 数据增强可以模拟图像姿态、光照和颜色变化，从而提升模型泛化能力。如果增强后验证集指标提升，说明模型对输入变化的鲁棒性增强；如果提升不明显，可能是预训练模型本身已经具有较强特征表达能力。

#### 不建议优先做：分割输入尺寸对比

| 输入尺寸 | batch size | mIoU | Dice | 显存占用 |
| ---- | ---------- | ---- | ---- | ---- |
| 256  | 8 或 4      | 待填写  | 待填写  | 待填写  |
| 320  | 4          | 待填写  | 待填写  | 待填写  |

这个消融能够体现显存和精度之间的权衡，但会额外增加分割训练时间。除非前两个消融已经完成，否则不建议作为本次报告的主消融。

---

### 4. 结论可以这样写

```latex
本实验基于 Oxford-IIIT Pet 数据集完成了宠物品种分类和宠物前景语义分割两个计算机视觉任务。分类任务中，通过使用预训练 CNN 并替换最后分类层，实现了 37 类宠物品种识别；分割任务中，通过将 trimap 标注转换为前景/背景 mask，并使用 DeepLabV3-MobileNetV3 进行像素级预测，实现了宠物主体区域提取。实验结果表明，迁移学习能够有效提升小规模数据集上的训练效率和模型性能，语义分割模型能够较好地定位宠物前景区域，但在边界细节和复杂背景下仍存在一定误差。
```

---

### 5. 改进方向可以写

建议写 4 点：

1. **使用更强的 backbone**
   例如 ResNet50、ConvNeXt-Tiny、SegFormer-B0，但需要更多显存和训练时间。

2. **改进分割损失函数**
   在 Cross Entropy Loss 基础上加入 Dice Loss 或 Focal Loss，缓解前景/背景像素不均衡问题。

3. **加入边界优化方法**
   对预测 mask 进行 CRF、形态学操作或边界损失优化，提高宠物毛发边缘分割质量。

4. **利用分割结果辅助分类**
   先分割宠物前景，再将背景去除后送入分类模型，分析背景干扰对分类准确率的影响。

---

## 六、建议你实际完成的实验清单

### 必做实验

| 编号    | 实验                       | 目标                  |
| ----- | ------------------------ | ------------------- |
| Exp-0 | 数据集统计与可视化                | 展示数据样例、类别分布、mask 样例 |
| Exp-1 | ResNet18 分类 baseline     | 得到基础分类结果            |
| Exp-2 | EfficientNet-B0 分类微调     | 得到主分类模型             |
| Exp-3 | DeepLabV3-MobileNetV3 分割 | 得到宠物前景分割模型          |
| Exp-4 | 分类和分割可视化                 | 生成报告图片              |

### 只选一个的轻量消融

| 编号    | 实验            | 目标         |
| ----- | ------------- | ---------- |
| Exp-5A | ResNet18 vs EfficientNet-B0 | 复用分类主线结果，形成模型对比 |
| Exp-5B | 有无数据增强对比      | 展示数据增强对泛化能力的影响     |

建议优先选择 **Exp-5A**。如果时间紧张，Exp-1 和 Exp-2 本身就能构成这个对比，不需要额外训练新模型。Grad-CAM、U-Net 对比和分割输入尺寸对比全部降为可选扩展，不影响标准版报告完整性。

---

## 七、推荐命令行设计

你可以让代码支持命令行参数，方便报告复现。

### 1. 训练分类模型

```bash
python src/train_cls.py \
  --model efficientnet_b0 \
  --img-size 224 \
  --batch-size 32 \
  --epochs 25 \
  --lr 1e-4 \
  --pretrained
```

### 2. 训练分割模型

```bash
python src/train_seg.py \
  --model deeplabv3_mobilenet \
  --img-size 320 \
  --batch-size 4 \
  --epochs 30 \
  --lr 1e-4 \
  --binary-mask
```

### 3. 评估分类模型

```bash
python src/eval_cls.py \
  --checkpoint outputs/checkpoints/best_cls.pth \
  --split test
```

### 4. 评估分割模型

```bash
python src/eval_seg.py \
  --checkpoint outputs/checkpoints/best_seg.pth \
  --split test
```

### 5. 生成可视化图片

```bash
python src/visualize.py \
  --cls-checkpoint outputs/checkpoints/best_cls.pth \
  --seg-checkpoint outputs/checkpoints/best_seg.pth \
  --num-samples 12
```

---

## 八、报告中建议放的图片和表格

### 图片清单

| 文件名                          | 内容                   |
| ---------------------------- | -------------------- |
| `fig_dataset_samples.png`    | 宠物图像样例               |
| `fig_mask_samples.png`       | 原图、原始 trimap、二值 mask |
| `fig_class_distribution.png` | 37 类类别分布             |
| `fig_cls_training_curve.png` | 分类训练曲线               |
| `fig_confusion_matrix.png`   | 分类混淆矩阵               |
| `fig_seg_training_curve.png` | 分割训练曲线               |
| `fig_seg_predictions.png`    | 分割预测可视化              |
| `fig_failure_cases.png`      | 失败案例分析               |

### 表格清单

| 表格  | 内容       |
| --- | -------- |
| 表 1 | 实验环境     |
| 表 2 | 数据集划分    |
| 表 3 | 分类模型训练配置 |
| 表 4 | 分割模型训练配置 |
| 表 5 | 分类实验结果   |
| 表 6 | 分割实验结果   |
| 表 7 | 消融实验结果   |

---

## 九、最推荐的最终实验组合

为了控制工作量，我建议你最终按这个组合完成：

### 标准最终版

| 模块          | 选择                                   |
| ----------- | ------------------------------------ |
| 数据集         | Oxford-IIIT Pet                      |
| 分类模型        | EfficientNet-B0                      |
| 分类 baseline | ResNet18                             |
| 分割模型        | DeepLabV3-MobileNetV3                |
| 分割方式        | 二分类前景分割                              |
| 分类指标        | Accuracy、Macro-F1、Confusion Matrix   |
| 分割指标        | Pixel Accuracy、mIoU、Dice             |
| 可视化         | 分类预测、错误样例、mask 对比、训练曲线               |
| 消融实验        | 优先 ResNet18 vs EfficientNet-B0；时间充裕再做有无数据增强 |

这个组合足够满足课程要求，并且不会让实现复杂度失控。实际执行时可以把 ResNet18 与 EfficientNet-B0 的对比直接作为消融实验，这样无需额外增加第三个分类训练分支。

---

## 十、你可以直接写进 README 的项目说明

```text
本项目基于 Oxford-IIIT Pet 数据集完成宠物图像品种分类与宠物前景语义分割两个任务。分类任务使用 ImageNet 预训练的 ResNet18 和 EfficientNet-B0 进行迁移学习，实现 37 类宠物品种识别；分割任务使用 DeepLabV3-MobileNetV3 对宠物图像进行前景/背景像素级分割。实验过程中完成了数据预处理、图像增强、模型微调、指标评估和结果可视化，并通过训练曲线、混淆矩阵和分割 mask 对比图分析模型性能。
```

---

## 十一、最终自检清单

提交前检查这些内容：

* [ ] 代码可以从零运行；
* [ ] 数据集读取路径不写死为个人绝对路径；
* [ ] README 里有运行命令；
* [ ] 保存了最佳模型权重；
* [ ] 有分类结果表格；
* [ ] 有分割结果表格；
* [ ] 有训练曲线；
* [ ] 有混淆矩阵；
* [ ] 有分割 mask 可视化；
* [ ] 报告中每段关键代码都有解释；
* [ ] 实验结果不是只写“成功运行”；
* [ ] 结论部分包含不足和改进方向。

[1]: https://www.robots.ox.ac.uk/~vgg/data/pets/?utm_source=chatgpt.com "Oxford-IIIT Pet Dataset"
[2]: https://docs.pytorch.org/vision/0.24/generated/torchvision.datasets.OxfordIIITPet.html?utm_source=chatgpt.com "OxfordIIITPet — Torchvision 0.24 documentation"
[3]: https://docs.pytorch.org/vision/main/models/generated/torchvision.models.resnet18.html?utm_source=chatgpt.com "resnet18 — Torchvision main documentation"
[4]: https://docs.pytorch.org/tutorials/beginner/transfer_learning_tutorial.html?utm_source=chatgpt.com "Transfer Learning for Computer Vision Tutorial"
[5]: https://albumentations.ai/docs/examples/pytorch-semantic-segmentation/?utm_source=chatgpt.com "pytorch semantic segmentation"
[6]: https://docs.pytorch.org/vision/main/models/generated/torchvision.models.segmentation.deeplabv3_mobilenet_v3_large.html?utm_source=chatgpt.com "deeplabv3_mobilenet_v3_large — Torchvision main ..."
