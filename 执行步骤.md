# Oxford-IIIT Pet 分类与前景分割项目执行步骤

本文档用于把 `方案.md` 落地为可执行清单。项目主线固定为：

- 数据集：Oxford-IIIT Pet
- 分类任务：37 类宠物品种分类
- 分类模型：ResNet18 baseline + EfficientNet-B0 主模型
- 分割任务：宠物前景 / 背景二分类语义分割
- 分割模型：DeepLabV3-MobileNetV3
- 轻量消融：优先使用 ResNet18 vs EfficientNet-B0
- 报告编译：全程使用 `xelatex`
- Python 环境：`D:\Anaconda3\envs\unet`

当前执行口径采用 **报告够用版**：优先保证实验能完整跑通、图表齐全、结果真实，不追求最高指标。正式训练轮数建议为：

| 实验 | epoch | 说明 |
| --- | --- | --- |
| ResNet18 分类 baseline | 15 | 作为基线和消融对比，不追求最优 |
| EfficientNet-B0 分类主模型 | 25 | 作为分类主结果，已完成完整训练 |
| DeepLabV3-MobileNetV3 分割 | 20 | 重点生成可分析的 mask、mIoU 和 Dice |

如果训练曲线最后几轮仍明显提升，可以补训 EfficientNet-B0 或分割模型到 25 epoch；如果指标已经趋于稳定，不再增加训练轮数。

## 总体交付物

最终至少应提交或保留以下材料：

- 可运行代码：`src/` 下的数据、模型、训练、评估、可视化脚本。
- 实验日志：分类和分割训练日志，建议保存为 CSV。
- 模型权重：ResNet18、EfficientNet-B0、DeepLabV3-MobileNetV3 的最佳权重。
- 指标文件：分类 Accuracy、Macro-F1；分割 Pixel Accuracy、mIoU、Dice。
- 报告图片：数据样例、mask 样例、类别分布、训练曲线、混淆矩阵、分割预测图、失败案例。
- LaTeX 报告：基于现有 `main.tex` 模板完成，并能用 `xelatex` 编译出 PDF。

建议目录：

```text
data/
src/
outputs/
  checkpoints/
  logs/
  figures/
  results/
report/
```

如果继续沿用当前根目录下的 `main.tex`，也可以先不移动报告模板，但报告图片路径应保持清晰一致。

---

## 第 0 步：确认环境与项目规范

### 目标

确认后续所有代码运行、实验记录和报告编译都在统一环境下完成，避免结果不可复现。

### 需要做的内容

1. 确认 conda 环境 `unet` 存在：

   ```powershell
   D:\Anaconda3\envs\unet\python.exe --version
   ```

2. 确认核心库是否可导入：

   ```powershell
   D:\Anaconda3\envs\unet\python.exe -c "import torch, torchvision, numpy, matplotlib, sklearn, pandas; print(torch.__version__, torchvision.__version__)"
   ```

3. 确认是否能调用 GPU：

   ```powershell
   D:\Anaconda3\envs\unet\python.exe -c "import torch; print(torch.cuda.is_available()); print(torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'CPU')"
   ```

4. 确认 LaTeX 编译命令可用：

   ```powershell
   xelatex main.tex
   ```

5. 阅读并遵守 `AGENTS.md` 中的项目规范，尤其是禁止批量删除文件或目录。

### 需要收集的报告材料

- [x] 实验环境表：操作系统、Python 版本、PyTorch 版本、TorchVision 版本、CUDA 是否可用、GPU 型号。
- [x] 可选：终端环境检查截图或文字记录。

### 第 0 步执行记录（2026-06-01）

| 检查项 | 命令或来源 | 结果 |
| --- | --- | --- |
| 操作系统 | `cmd /c ver` / Python `platform.platform()` | Microsoft Windows 10.0.26100.4946；Python 记录为 `Windows-10-10.0.26100-SP0` |
| Python 环境 | `D:\Anaconda3\envs\unet\python.exe --version` | `Python 3.10.20` |
| PyTorch | Python import 检查 | `torch 2.11.0+cu126`，可正常导入 |
| TorchVision | Python import 检查 | `torchvision 0.26.0+cu126`，可正常导入 |
| NumPy | Python import 检查 | `numpy 1.23.5`，可正常导入 |
| Matplotlib | Python import 检查 | `matplotlib 3.6.2`，可正常导入 |
| scikit-learn | Python import 检查 | `sklearn 1.7.2`，已安装并可正常导入 |
| Pandas | Python import 检查 | `pandas 2.3.3`，已安装并可正常导入 |
| CUDA 可用性 | `torch.cuda.is_available()` | `True` |
| CUDA / cuDNN | `torch.version.cuda` / `torch.backends.cudnn.version()` | CUDA `12.6`；cuDNN `91002` |
| GPU 型号 | `torch.cuda.get_device_name(0)` | `NVIDIA GeForce RTX 4060 Laptop GPU` |
| LaTeX 编译 | `xelatex main.tex` | 编译成功，生成 `main.pdf`，日志显示 `Output written on main.pdf (3 pages).` |
| 项目规范 | 阅读 `AGENTS.md` | 已确认：使用 `unet` 环境、报告使用 `xelatex`、禁止批量删除文件或目录、不虚构实验指标。 |
| 缺失依赖补装 | `D:\Anaconda3\envs\unet\python.exe -m pip install scikit-learn pandas` | 安装成功；同时安装依赖 `scipy 1.15.3`、`joblib 1.5.3`、`threadpoolctl 3.6.0`、`pytz 2026.2`、`tzdata 2026.2`。 |

补装后复验命令：

```powershell
D:\Anaconda3\envs\unet\python.exe -c "import torch, torchvision, numpy, matplotlib, sklearn, pandas; print(torch.__version__, torchvision.__version__, numpy.__version__, matplotlib.__version__, sklearn.__version__, pandas.__version__)"
```

复验输出：`2.11.0+cu126 0.26.0+cu126 1.23.5 3.6.2 1.7.2 2.3.3`。第 0 步所需核心库现在均可正常导入。

### 检查与验收清单

- [x] `D:\Anaconda3\envs\unet\python.exe` 能正常运行。
- [x] PyTorch 和 TorchVision 能正常导入。
- [x] 若本机有 NVIDIA GPU，`torch.cuda.is_available()` 应为 `True`；如果为 `False`，需要记录原因并降低 batch size。
- [x] `xelatex main.tex` 至少能编译现有模板，不因字体或宏包缺失而失败。
- [x] 报告中能填写一张完整的“实验环境”表。

---

## 第 1 步：建立项目目录与配置文件

### 目标

把数据、代码、输出和报告材料分开管理，后续训练结果可以直接进入报告。

### 需要做的内容

1. 建立或确认以下目录：

   ```text
   data/
   src/
   outputs/checkpoints/
   outputs/logs/
   outputs/figures/
   outputs/results/
   ```

2. 建议在 `src/config.py` 中集中保存基础配置：

   - 随机种子：如 `42`
   - 数据目录：如 `data/oxford-iiit-pet`
   - 分类输入尺寸：`224`
   - 分割输入尺寸：`320` 或 `256`
   - 分类 batch size：优先 `32`，显存不足改 `16`
   - 分割 batch size：优先 `4`，显存不足改 `2`
   - ResNet18 epoch：`15`
   - EfficientNet-B0 epoch：`25`
   - DeepLabV3-MobileNetV3 epoch：`20`
   - 输出目录：`outputs/`

3. 所有脚本应支持命令行参数，避免把实验配置写死在代码里。

### 需要收集的报告材料

- [x] 项目目录结构截图或以文本形式写入 README。
- [x] 关键配置表：分类训练配置、分割训练配置。

### 检查与验收清单

- [x] 目录结构清晰，输出文件不会混在根目录。
- [x] 代码中没有硬编码个人绝对数据路径；`D:\Anaconda3\envs\unet` 只作为运行环境路径，不作为数据路径。
- [x] 随机种子在数据划分、训练初始化中被设置。
- [x] 后续训练命令可以通过参数复现实验。

执行记录：已建立 `data/`、`src/`、`outputs/checkpoints/`、`outputs/logs/`、`outputs/figures/`、`outputs/results/`，并新增 `src/config.py` 集中管理路径、随机种子、输入尺寸、batch size 和输出目录。后续训练、评估和可视化脚本需要继续沿用该配置，并提供命令行参数覆盖默认值。

---

## 第 2 步：数据集下载、读取与划分

### 目标

使用 Oxford-IIIT Pet 数据集完成分类标签和分割 mask 的读取，并固定训练集、验证集、测试集划分。

### 需要做的内容

建议拆成以下小步骤执行：

#### 2.1 数据获取与缓存确认

- [x] 确认 `data/` 下是否已有 Oxford-IIIT Pet 数据。
- [x] 若没有数据，使用 TorchVision 自动下载或手动放置数据。
- [x] 记录数据来源和存放路径，但报告和代码中不要写死个人绝对路径。

#### 2.2 分类标签读取检查

- [x] 读取 `target_types="category"`。
- [x] 检查类别数量是否为 37。
- [x] 抽查若干样本，确认图像与类别标签对应。

#### 2.3 分割标注读取检查

- [x] 读取 `target_types="segmentation"`。
- [x] 对若干 mask 执行 `np.unique(mask)`。
- [x] 记录原始 trimap 像素值，为第 4 步转换规则做依据。

#### 2.4 固定划分与统计输出

- [x] 将官方 `trainval` 固定随机种子划分为训练集和验证集。
- [x] 保留官方 `test` 作为最终测试集。
- [x] 输出样本数量、类别分布、mask 像素值统计。

1. 使用 TorchVision 的 `OxfordIIITPet` 读取数据。

2. 分类任务读取：

   - `target_types="category"`
   - 目标类别数为 37

3. 分割任务读取：

   - `target_types="segmentation"`
   - 检查原始 trimap 像素值，通常为 `1`、`2`、`3`

4. 数据划分方式：

   - 官方 `trainval` 再按 8:2 分为训练集和验证集
   - 官方 `test` 仅用于最终测试
   - 训练集、验证集、测试集数量都应记录

5. 编写数据统计脚本，建议输出：

   - 每个 split 的样本数
   - 37 类类别名称和类别数量
   - 每类训练样本数量
   - 分割 mask 的像素值集合

### 需要收集的报告材料

- [x] 数据集划分表：训练集、验证集、测试集样本数。
- [x] 类别分布图：`outputs/figures/fig_class_distribution.png`
- [x] 数据样例图：`outputs/figures/fig_dataset_samples.png`
- [x] 原始 trimap 检查结果：`np.unique(mask)` 的记录。

### 第 2 步执行记录（2026-06-01）

运行命令：

```powershell
D:\Anaconda3\envs\unet\python.exe src\inspect_data.py --scan-all-trimaps
```

核查结果：

| 项目 | 结果 |
| --- | --- |
| 数据缓存 | `data/oxford-iiit-pet/` 下存在 `images/`、`annotations/`、`images.tar.gz`、`annotations.tar.gz` |
| 读取方式 | TorchVision `OxfordIIITPet`，分类使用 `target_types="category"`，分割使用 `target_types="segmentation"` |
| 类别数量 | 37 类 |
| 官方 `trainval` 数量 | 3680 |
| 固定训练集数量 | 2944 |
| 固定验证集数量 | 736 |
| 官方测试集数量 | 3669 |
| 随机种子 | `42` |
| 验证集比例 | `0.2` |
| trimap 像素值 | `trainval` 和 `test` 全量扫描均为 `[1, 2, 3]`；含义记录为 `1=pet foreground`、`2=background`、`3=border` |

已生成文件：

- `outputs/results/data_summary.json`
- `outputs/results/split_indices_seed42.json`
- `outputs/results/class_distribution.csv`
- `outputs/figures/fig_class_distribution.png`
- `outputs/figures/fig_dataset_samples.png`

结论：第 2 步已完成，数据读取、标签检查、trimap 检查、固定划分和统计输出均满足要求，可以进入第 3 步“图像预处理与数据增强”。

### 检查与验收清单

- [x] 能成功读取 `trainval` 和 `test`。
- [x] 分类标签范围正确，应为 37 类。
- [x] `trainval` 的 8:2 划分固定可复现，多次运行样本数量一致。
- [x] 测试集没有参与训练和模型选择。
- [x] 类别分布图可读，横轴标签不严重重叠。
- [x] 随机展示的数据图像、类别标签和文件对应关系正确。

---

## 第 3 步：图像预处理与数据增强

### 目标

为分类和分割分别建立合理的数据预处理流程，保证输入尺寸、归一化方式和 mask 处理正确。

### 需要做的内容

建议拆成以下小步骤执行：

#### 3.1 分类预处理流水线

- [x] 实现训练集 transform。
- [x] 实现验证集 / 测试集 transform。
- [x] 单独检查分类 batch 的图像形状和标签形状。

#### 3.2 分割预处理流水线

- [x] 实现图像 resize、归一化。
- [x] 实现 mask 最近邻 resize。
- [x] 保证图像和 mask 的随机空间变换同步。

执行记录：

- 已新增 `src/data_transforms.py`，提供 `build_classification_transform` 和 `build_segmentation_transform`。
- 分类 transform 检查结果：训练、验证、测试 batch 图像形状均为 `[B, 3, 224, 224]`，标签形状为 `[B]`。
- 分割 transform 检查结果：训练、验证、测试 batch 图像形状均为 `[B, 3, 320, 320]`，mask 形状为 `[B, 320, 320]`。
- mask resize 使用最近邻插值，检查到的 mask 值仍为整数 trimap 值 `[1, 2, 3]`；trimap 到二值 mask 的转换保留到第 4 步执行。

#### 3.3 预处理结果抽查

- [x] 可视化处理前后的图像。
- [x] 可视化处理后的 mask。
- [x] 确认 mask 没有被插值成小数或异常类别值。

已完成记录：

- 抽查脚本：`src/inspect_preprocessing.py`
- 抽查图片：`outputs/figures/fig_preprocessing_check.png`
- 抽查结果：`outputs/results/preprocessing_check.json`
- 抽查结论：分类预处理输出为 `[3, 224, 224]`，分割图像输出为 `[3, 320, 320]`，分割 mask 输出为 `[320, 320]`；当前阶段 mask 仍保持原始 trimap 整数值 `[1, 2, 3]`，未被归一化或插值成小数，二值化留到第 4 步处理。

1. 分类图像预处理：

   - resize / crop 到 `224 x 224`
   - 使用 ImageNet mean/std 归一化
   - 训练集可使用随机水平翻转、随机裁剪、颜色扰动等轻量增强
   - 验证集和测试集只做确定性 resize / center crop / normalize

2. 分割图像预处理：

   - 输入尺寸建议 `320 x 320`，显存紧张可用 `256 x 256`
   - 图像 resize 使用双线性插值
   - mask resize 必须使用最近邻插值
   - 图像和 mask 的空间增强必须同步

3. 数据增强不要过强，避免破坏宠物主体和 mask 对齐。

### 需要收集的报告材料

- [x] 分类预处理说明：输入尺寸、归一化、训练增强方式。
- [x] 分割预处理说明：输入尺寸、mask 最近邻插值、同步变换。
- [x] 可选：增强前后样例图。

报告材料记录：

- 分类预处理说明已整理在 `README.md` 的 Step 3 进度记录中，代码实现见 `src/data_transforms.py` 和 `src/data_loaders.py`。
- 分割预处理说明已整理在 `README.md` 的 Step 3 进度记录中，明确记录图像双线性 resize、mask 最近邻 resize、同步空间变换，以及二值 mask 转换留到第 4 步。
- 批量检查结果已保存到 `outputs/results/dataloader_check.json`，可作为报告中预处理与 dataloader 验收材料。

### 检查与验收清单

- [x] 分类 dataloader 输出图像张量形状为 `[B, 3, 224, 224]`。
- [x] 分割 dataloader 输出图像张量形状为 `[B, 3, H, W]`，mask 形状为 `[B, H, W]`。
- [x] mask 不应被归一化，不应出现小数类别值。
- [x] 分割增强后，原图和 mask 仍然对齐。
- [x] 训练集使用随机增强时，验证集和测试集不使用随机增强。

---

## 第 4 步：trimap 转二值前景 mask

### 目标

把 Oxford-IIIT Pet 的三值 trimap 转换为宠物前景 / 背景二分类 mask，用于 DeepLabV3 训练。

### 需要做的内容

建议拆成以下小步骤执行：

#### 4.1 原始 trimap 值确认

- [x] 随机抽查多个样本的 `np.unique(mask)`。
- [x] 记录 pet、background、border 的实际像素值对应关系。

#### 4.2 转换规则确定

- [x] 选择“边界忽略”或“边界并入前景”。
- [x] 在代码、指标计算和报告说明中使用同一套规则。

#### 4.3 转换函数实现与单元抽查

- [x] 对单张 mask 测试转换函数。
- [x] 检查转换后只包含合法类别值。
- [x] 对比原图、原始 trimap、二值 mask，确认前景和背景没有反置。

#### 4.4 loss 与指标同步

- [x] 本项目未采用 `255` 忽略边界方案，因此无需设置 `ignore_index`。
- [x] 如果边界并入前景，loss 和指标按普通二分类处理。

### 当前进度记录

- 原始 trimap 抽查记录已保存到 `outputs/results/trimap_rule_step4.json`，12 个样本均观察到原始像素值 `[1, 2, 3]`。
- 本项目采用方案 B：原始值 `2` 映射为背景 `0`，原始值 `1` 和 `3` 映射为前景 `1`；不使用 `ignore_index=255`。
- 二值转换函数已在 `src/data_transforms.py` 中实现为 `convert_trimap_to_binary_mask`，分割数据集默认启用 `binary_mask=True`。
- loss 与指标已在 `src/segmentation_utils.py` 中按普通二分类同步：`CrossEntropyLoss` 不设置 `ignore_index`，指标计算只接受 `0/1` 标签。
- Step 4 可视化检查脚本为 `src/inspect_step4_masks.py`，已生成 `outputs/figures/fig_mask_samples.png` 和 `outputs/results/mask_conversion_check.json`。
- `mask_conversion_check.json` 检查结果：`sample_count=12`，`raw_values_ok=true`，`binary_values_ok=true`，`sample_count_ok=true`；所有二值 mask 的取值均为 `[0, 1]`。

1. 先用 `np.unique(mask)` 检查原始像素值。

2. 明确转换规则，并在报告中写清楚。推荐二选一：

   - 方案 A：pet 为前景，background 为背景，border 忽略为 `255`
   - 方案 B：pet 和 border 合并为前景，background 为背景

3. 若采用忽略边界：

   - loss 使用 `CrossEntropyLoss(ignore_index=255)`
   - 指标计算时忽略 `255`

4. 若采用边界并入前景：

   - 输出类别为 2 类：背景 `0`，前景 `1`
   - loss 使用普通 `CrossEntropyLoss`

建议课程报告优先使用方案 B，简单稳定；如果希望更严谨，可使用方案 A。

### 需要收集的报告材料

- [x] mask 转换规则说明。
- [x] 原图、原始 trimap、二值 mask 对比图：`outputs/figures/fig_mask_samples.png`
- [x] mask 像素值检查记录。

### 检查与验收清单

- [x] 转换后 mask 只包含合法值：`0/1`，或 `0/1/255`。
- [x] 背景和前景没有反置；宠物主体应显示为前景。
- [x] mask 样例图至少展示 6 到 12 张，有不同背景和宠物姿态。
- [x] 本项目未使用 `255` 忽略边界；训练 loss 和指标代码均按普通 `0/1` 二分类处理。

---

## 第 5 步：实现分类模型 ResNet18 与 EfficientNet-B0

### 目标

构建两个迁移学习分类模型，用于 37 类宠物品种分类，并形成主模型和 baseline 对比。

### 需要做的内容

建议拆成以下小步骤执行：

#### 5.1 ResNet18 baseline 实现

- [ ] 加载 `ResNet18_Weights.DEFAULT`。
- [ ] 替换最后 `fc` 层为 37 类输出。
- [ ] 用一个小 batch 检查输出形状。

#### 5.2 EfficientNet-B0 主模型实现

- [ ] 加载 `EfficientNet_B0_Weights.DEFAULT`。
- [ ] 替换 classifier 最后一层为 37 类输出。
- [ ] 用一个小 batch 检查输出形状。

#### 5.3 通用训练接口准备

- [ ] 统一模型创建函数的参数，如 `model_name`、`num_classes`、`pretrained`。
- [ ] 统一 checkpoint 保存命名规则。
- [ ] 确认两个模型使用同一套类别映射。

1. 在 `src/models_cls.py` 中实现模型构建函数。

2. ResNet18：

   - 使用 `ResNet18_Weights.DEFAULT`
   - 替换最后 `fc` 层为 37 类输出

3. EfficientNet-B0：

   - 使用 `EfficientNet_B0_Weights.DEFAULT`
   - 替换 classifier 最后一层为 37 类输出

4. 训练策略：

   - 优先全模型微调
   - 若训练慢，可先冻结 backbone 训练分类头，再解冻微调

5. 输出 logits 形状应为 `[B, 37]`。

### 需要收集的报告材料

- [ ] 模型结构说明：预训练权重、替换输出层、迁移学习策略。
- [ ] 分类训练配置表：模型、输入尺寸、batch size、epoch、optimizer、learning rate、loss、scheduler。
- [ ] 可选：模型参数量或简要结构截图。

### 检查与验收清单

- [x] ResNet18 和 EfficientNet-B0 都能完成一次前向传播。
- [x] 输出维度为 37。
- [x] 预训练权重加载成功。
- [x] 损失函数使用 `CrossEntropyLoss`。
- [x] 模型保存接口可分别保存两个模型的最佳权重。

执行记录：已新增 `src/models_cls.py`，实现 `build_resnet18`、`build_efficientnet_b0` 和统一入口 `build_classification_model`。使用 `TORCH_HOME=outputs/torch_cache` 临时缓存 TorchVision 官方预训练权重，验证 ResNet18 与 EfficientNet-B0 均可完成一次前向传播，测试 batch 输出形状均为 `[2, 37]`；`CrossEntropyLoss` 可正常计算；checkpoint 路径分别为 `outputs/checkpoints/best_cls_resnet18.pth` 与 `outputs/checkpoints/best_cls_efficientnet_b0.pth`。

---

## 第 6 步：训练 ResNet18 分类 baseline

### 目标

获得基础分类结果，为后续 EfficientNet-B0 主模型和消融对比提供基线。

### 需要做的内容

建议拆成以下小步骤执行：

#### 6.1 训练前检查

- [x] 确认 ResNet18 前向传播正常。
- [x] 确认分类 train / val dataloader 输出正常。
- [x] 确认日志文件路径和 checkpoint 路径存在。

#### 6.2 小规模试跑

- [x] 先跑 1 个 epoch。
- [x] 检查 train loss、val loss、val accuracy、val macro-F1 都能输出。
- [x] 检查最佳权重保存逻辑能触发。

#### 6.3 完整 baseline 训练

- [x] 按正式参数完成 ResNet18 训练。
- [x] 保存完整 CSV 日志。
- [x] 记录最佳 epoch 和最佳验证指标。

1. 推荐命令：

   ```powershell
   D:\Anaconda3\envs\unet\python.exe src\train_cls.py --model resnet18 --img-size 224 --batch-size 32 --epochs 15 --lr 1e-4 --pretrained
   ```

2. 如果显存不足，将 batch size 改为 `16`。

3. 每个 epoch 记录：

   - train loss
   - val loss
   - val accuracy
   - val macro-F1
   - learning rate

4. 按验证集 Macro-F1 或 Accuracy 保存最佳模型。

### 需要收集的报告材料

- [x] 训练日志：`outputs/logs/resnet18_cls_log.csv`
- [x] 最佳权重：`outputs/checkpoints/best_cls_resnet18.pth`
- [x] 训练过程截图或日志片段。
- [x] ResNet18 验证集最佳 epoch 记录。

### 检查与验收清单

- [x] 训练过程中 loss 不为 NaN。
- [x] 训练日志字段完整，每个 epoch 都有记录。
- [x] 最佳模型权重成功保存。
- [x] 验证集准确率明显高于随机猜测水平。37 类随机猜测约为 2.7%，ResNet18 迁移学习结果应远高于该值。
- [x] 指标正常，无需按异常偏低流程排查；已记录真实训练输出。

### 第 6 步执行记录（2026-06-01）

实际训练命令采用 `ResNet18_Weights.DEFAULT` 预训练权重、输入尺寸 `224`、batch size `32`、学习率 `1e-4`，在 CUDA 上完成 ResNet18 baseline 训练。文档推荐口径为 15 epoch，实际日志保留了 20 epoch 的完整训练记录；最佳验证结果出现在第 15 轮，因此可作为报告中的 ResNet18 baseline 最优结果。

已生成文件：

- `outputs/logs/resnet18_cls_log.csv`
- `outputs/logs/resnet18_cls_train_stdout.txt`
- `outputs/logs/resnet18_cls_train_stderr.txt`
- `outputs/checkpoints/best_cls_resnet18.pth`

复验记录：

| 检查项 | 结果 |
| --- | --- |
| 训练集 / 验证集数量 | `2944 / 736` |
| CSV 日志行数 | 20 个 epoch |
| 日志字段 | `epoch`、`train_loss`、`val_loss`、`val_accuracy`、`val_macro_f1`、`lr`、`epoch_seconds` |
| NaN 检查 | 训练 loss、验证 loss、Accuracy、Macro-F1、学习率和耗时均无 NaN |
| 最佳 epoch | 15 |
| 最佳验证 Accuracy | `0.930707` |
| 最佳验证 Macro-F1 | `0.930059` |
| 总训练耗时 | 约 `765.40` 秒，约 `12.76` 分钟 |
| checkpoint 元数据 | `model=resnet18`，`img_size=224`，`num_classes=37`，`pretrained=True` |
| checkpoint 加载 | 可用 `build_classification_model("resnet18", pretrained=False)` 严格加载，无 missing / unexpected keys |
| 前向复验 | 随机输入 `[2, 3, 224, 224]` 输出 `[2, 37]` |

结论：第 6 步已完成，ResNet18 baseline 的日志、最佳权重和验证指标均满足进入第 7 步的条件。第 7 步训练 EfficientNet-B0 时应继续使用相同数据划分、输入尺寸、batch size 和学习率，以便后续进行公平消融对比。

---

## 第 7 步：训练 EfficientNet-B0 分类主模型

### 目标

获得项目的主分类模型结果，并与 ResNet18 构成轻量消融实验。

### 需要做的内容

建议拆成以下小步骤执行：

#### 7.1 训练前检查

- [x] 确认 EfficientNet-B0 前向传播正常。2026-06-02 已用 2 张样本前向检查，输出 logits 形状为 `[2, 37]`，数值有限。
- [x] 确认输入尺寸、batch size 和 ResNet18 对比设置一致。当前使用 `img_size=224`、`batch_size=32`、`lr=1e-4`，与 ResNet18 baseline 设置一致。
- [x] 确认输出 checkpoint 不会覆盖 ResNet18 权重。EfficientNet-B0 目标路径为 `outputs/checkpoints/best_cls_efficientnet_b0.pth`，ResNet18 权重仍为 `outputs/checkpoints/best_cls_resnet18.pth`。

#### 7.2 小规模试跑

- [x] 先跑 1 个 epoch。
- [x] 检查 loss 和验证指标正常。试跑结果：`train_loss=2.938021`，`val_loss=1.745005`，`val_accuracy=0.750000`，`val_macro_f1=0.731191`。
- [x] 若显存不足，先降低 batch size，并记录调整原因。本次使用 CUDA + AMP，`batch_size=32` 正常完成，未发生显存不足，因此未降低 batch size。

7.1-7.2 阶段记录：

- 执行命令：

  ```powershell
  D:\Anaconda3\envs\unet\python.exe src\train_cls.py --model efficientnet_b0 --img-size 224 --batch-size 32 --epochs 1 --lr 1e-4 --pretrained --amp --num-workers 0
  ```

- 运行设备：`cuda`。
- 数据规模：训练集 `2944`，验证集 `736`。
- 1 epoch 训练耗时：脚本记录 `83.22s`，端到端约 `103s`。
- 日志文件：`outputs/logs/efficientnet_b0_cls_log.csv`。
- 试跑 checkpoint：`outputs/checkpoints/best_cls_efficientnet_b0.pth`。
- 合规性检查：未发现不合规项。需要注意的是，当前 `best_cls_efficientnet_b0.pth` 仅为 1 epoch 试跑产生的临时最佳权重，不应作为最终报告指标；第 7.3 完整训练完成后应以完整训练日志和权重为准。

#### 7.3 完整主模型训练

- [x] 按正式参数完成 EfficientNet-B0 训练。2026-06-02 已完成 25 epoch 完整训练。
- [x] 保存完整 CSV 日志。日志文件为 `outputs/logs/efficientnet_b0_cls_log.csv`，共 25 条 epoch 记录。
- [x] 记录最佳 epoch、最佳验证指标和训练耗时。最佳 epoch 为 13，验证集 Accuracy 为 `0.937500`，Macro-F1 为 `0.935778`，完整训练累计约 `1010.81s`。

#### 7.4 与 baseline 对齐

- [x] 确认使用同一数据划分。EfficientNet-B0 与 ResNet18 均使用 `seed=42`，官方 `trainval` 按 8:2 固定划分为训练集 `2944` 和验证集 `736`。
- [x] 确认使用同一测试集做最终评估。官方 `test` split 共 `3669` 张，仅预留给第 9 步最终评估，Step 7.4 未使用测试集调参。
- [x] 为第 12 步消融表预留对比数据。已保存 `outputs/results/cls_ablation_val_summary.json` 和 `outputs/results/cls_ablation_val_summary.csv`。

7.3-7.4 阶段记录：

- 完整训练命令：

  ```powershell
  D:\Anaconda3\envs\unet\python.exe src\train_cls.py --model efficientnet_b0 --img-size 224 --batch-size 32 --epochs 25 --lr 1e-4 --pretrained --amp --num-workers 0
  ```

- 最佳权重：`outputs/checkpoints/best_cls_efficientnet_b0.pth`。
- 完整训练日志：`outputs/logs/efficientnet_b0_cls_log.csv`。
- 分类训练曲线：`outputs/figures/fig_cls_training_curve.png`。
- 对齐摘要：`outputs/results/cls_ablation_val_summary.json`、`outputs/results/cls_ablation_val_summary.csv`。
- 与 ResNet18 的验证集对齐结果：ResNet18 最佳验证 Accuracy / Macro-F1 为 `0.930707 / 0.930059`；EfficientNet-B0 最佳验证 Accuracy / Macro-F1 为 `0.937500 / 0.935778`。
- 合规性检查：未发现不合规项。当前对比数据仅为验证集阶段摘要，最终测试集 Accuracy 和 Macro-F1 应在第 9 步加载两个最佳 checkpoint 后统一评估得到。

1. 推荐命令：

   ```powershell
   D:\Anaconda3\envs\unet\python.exe src\train_cls.py --model efficientnet_b0 --img-size 224 --batch-size 32 --epochs 25 --lr 1e-4 --pretrained
   ```

2. 每个 epoch 记录内容同 ResNet18。

3. 保存验证集最优模型。

4. 训练完成后，比较 EfficientNet-B0 和 ResNet18 的：

   - 最佳验证 Accuracy
   - 最佳验证 Macro-F1
   - 收敛速度
   - 训练耗时

### 需要收集的报告材料

- [x] 训练日志：`outputs/logs/efficientnet_b0_cls_log.csv`
- [x] 最佳权重：`outputs/checkpoints/best_cls_efficientnet_b0.pth`
- [x] 分类训练曲线：`outputs/figures/fig_cls_training_curve.png`
- [x] 消融对比表初稿：ResNet18 vs EfficientNet-B0，见 `outputs/results/cls_ablation_val_summary.csv`。

### 检查与验收清单

- [x] EfficientNet-B0 训练可完整跑完。
- [x] 最佳权重成功保存。
- [x] 能与 ResNet18 形成同一测试集上的公平对比。
- [x] 若 EfficientNet-B0 没有超过 ResNet18，也可以接受，但报告中必须分析可能原因，如训练轮数、学习率、数据增强、模型过拟合或样本差异。当前验证集上 EfficientNet-B0 略高于 ResNet18，但最终结论仍以后续第 9 步官方测试集评估为准。

---

## 第 8 步：实现并训练 DeepLabV3-MobileNetV3 分割模型

### 目标

完成宠物前景 / 背景像素级分割，得到分割指标和 mask 可视化结果。

### 需要做的内容

建议拆成以下小步骤执行：

#### 8.1 分割模型构建

- [x] 创建 DeepLabV3-MobileNetV3 模型。实现位置：`src/models_seg.py`。
- [x] 加载预训练权重。训练 checkpoint 记录 `pretrained=True`。
- [x] 将输出头改为 2 类。主输出头和 auxiliary head 均替换为 2 类输出。
- [x] 用一个小 batch 做前向传播检查。修复后检查输出 logits 形状为 `[2, 2, 64, 64]`。

#### 8.2 分割训练脚本联调

- [x] 确认 dataloader 输出图像和 mask 尺寸匹配。默认数据根已自动指向 `data/step6_data`，验证 batch 输出图像 `[2, 3, 64, 64]`、mask `[2, 64, 64]`。
- [x] 确认 loss 输入为 logits，标签为类别索引。mask 验证值仅包含 `0/1`，loss 使用 `CrossEntropyLoss`。
- [x] 确认 AMP、optimizer 和日志保存逻辑正常。训练脚本支持 AMP，当前训练记录使用 AdamW、学习率固定为 `1e-4`，未使用 scheduler，每个 epoch 写入 CSV 日志。

#### 8.3 小规模试跑

- [x] 先跑 1 到 2 个 epoch。日志中第 1 epoch 已正常记录 train loss、val loss、Pixel Accuracy、mIoU 和 Dice。
- [x] 检查 loss、mIoU、Dice 是否能正常输出。30 个 epoch 均有完整指标记录。
- [x] 观察预测 mask 是否不是全背景或全前景。使用最佳 checkpoint 抽查验证 batch，预测 mask 与目标 mask 均包含 `[0, 1]`。

#### 8.4 完整训练与保存

- [x] 按正式 epoch 完整训练。已完成 30 epoch。
- [x] 按验证集 mIoU 或 Dice 保存最佳权重。最佳 epoch 为第 23 epoch，验证集 mIoU 为 `0.926738`，Dice 为 `0.955286`。
- [x] 保存训练日志和训练曲线。已补齐规范日志文件和分割训练曲线。

1. 在 `src/models_seg.py` 中实现 DeepLabV3-MobileNetV3 构建函数。

2. 使用 TorchVision 的 `deeplabv3_mobilenet_v3_large`。

3. 修改输出头，使输出类别数为 2。

4. 推荐命令：

   ```powershell
   D:\Anaconda3\envs\unet\python.exe src\train_seg.py --model deeplabv3_mobilenet --data-root data\step6_data --img-size 320 --batch-size 4 --epochs 30 --lr 1e-4 --pretrained --binary-mask
   ```

5. 如果显存不足：

   - 先把 batch size 改为 `2`
   - 再把输入尺寸从 `320` 改为 `256`

6. 每个 epoch 记录：

   - train loss
   - val loss
   - val pixel accuracy
   - val mIoU
   - val Dice

7. 按验证集 mIoU 或 Dice 保存最佳模型。

### 需要收集的报告材料

- [x] 训练日志：`outputs/logs/deeplabv3_mobilenet_seg_log.csv`。原始保留文件：`outputs/logs/deeplabv3_mobilenet_seg_log_epoch30.csv`。
- [x] 最佳权重：`outputs/checkpoints/best_seg_deeplabv3_mobilenet.pth`。原始保留文件：`outputs/checkpoints/best_seg_deeplabv3_mobilenet_epoch23_miou09267.pth`。
- [x] 分割训练曲线：`outputs/figures/fig_seg_training_curve.png`。
- [x] 训练配置表：输入尺寸 `320`，batch size `4`，epoch `30`，loss 为 `CrossEntropyLoss`，optimizer 为 `AdamW`，当前训练 checkpoint 记录 `pretrained=True`。

### 检查与验收清单

- [x] 模型输出空间尺寸能与 mask 对齐，或经过插值后与 mask 对齐。`train_seg.py` 中已实现 logits 到 target 尺寸的插值对齐。
- [x] 输出 logits 形状为 `[B, 2, H, W]`。小 batch 前向检查输出 `[2, 2, 64, 64]`。
- [x] 训练 loss 不为 NaN。30 个 epoch 训练日志均为正常数值。
- [x] mIoU 和 Dice 能正常计算。最佳验证 mIoU / Dice 为 `0.926738 / 0.955286`。
- [x] 可视化结果能看出宠物主体区域，而不是全背景或全前景。抽查预测 mask 同时包含背景和前景类别。
- [x] 若显存不足，调整 batch size 和输入尺寸后能继续训练，并记录调整原因。当前按 `320`、batch size `4` 完成训练，未记录显存不足调整。

Step 8 修复与核查记录：

- 已将 `src/config.py` 的默认数据根修正为优先使用可读的 `data/step6_data`，避免默认读取 `data/oxford-iiit-pet` 时遇到权限问题。
- 已将 `src/models_seg.py` 中 DeepLabV3-MobileNetV3 的 auxiliary head 构建固定为启用状态，保证 `pretrained=False` 构建模型时也能加载训练得到的 checkpoint。
- 已补齐规范命名副本：`outputs/logs/deeplabv3_mobilenet_seg_log.csv` 和 `outputs/checkpoints/best_seg_deeplabv3_mobilenet.pth`。
- 已新增并运行 `src/plot_seg_training_curve.py`，生成 `outputs/figures/fig_seg_training_curve.png`。
- 修复后验证：默认 dataloader 可读取验证集 `736` 张；规范 checkpoint 可直接加载；最佳 checkpoint 记录第 `23` epoch，`val_miou=0.926738`，`val_dice=0.955286`。

---

## 第 9 步：分类模型最终评估

### 目标

在官方测试集上评估 ResNet18 和 EfficientNet-B0，得到报告中的最终分类结果。

### 需要做的内容

建议拆成以下小步骤执行：

#### 9.1 加载 checkpoint

- [x] 分别加载 ResNet18 和 EfficientNet-B0 最佳权重。
- [x] ResNet18 的最终评估权重读取第 6.3 步完整训练生成的 `outputs/checkpoints/best_cls_resnet18.pth`。
- [x] 该权重由 `src/train_cls.py --model resnet18` 按验证集 Macro-F1 保存，当前记录的最佳 epoch 为第 15 轮。
- [x] 确认模型结构参数与 checkpoint 匹配。

#### 9.2 测试集推理

- [x] 使用官方 `test` split。
- [x] 关闭随机增强。
- [x] 保存每张图像的真实标签、预测标签和预测置信度。

#### 9.3 指标与图像生成

- [x] 计算 Accuracy、Macro-F1 和每类指标。
- [x] 生成 37 类混淆矩阵。
- [x] 挑选分类正确和分类错误样例。

1. 评估 ResNet18：

   ```powershell
   D:\Anaconda3\envs\unet\python.exe src\eval_cls.py --checkpoint outputs\checkpoints\best_cls_resnet18.pth --model resnet18 --split test
   ```

   其中 `--checkpoint outputs\checkpoints\best_cls_resnet18.pth` 即读取当前已经完成的 ResNet18 baseline 最佳权重，用于官方 `test` split 上的最终分类评估。

2. 评估 EfficientNet-B0：

   ```powershell
   D:\Anaconda3\envs\unet\python.exe src\eval_cls.py --checkpoint outputs\checkpoints\best_cls_efficientnet_b0.pth --model efficientnet_b0 --split test
   ```

3. 输出并保存：

   - Accuracy
   - Macro-F1
   - 每类 precision / recall / F1
   - 混淆矩阵
   - Top-5 Accuracy 可选

4. 生成混淆矩阵图：

   - 文件名：`outputs/figures/fig_confusion_matrix.png`
   - 类别名称过多时可以调整字体或只显示编号，并在报告中说明

### 需要收集的报告材料

- [x] 分类指标 JSON：`outputs/results/cls_metrics_resnet18.json`
- [x] 分类指标 JSON：`outputs/results/cls_metrics_efficientnet_b0.json`
- [x] 分类结果表：模型、是否预训练、数据增强、Accuracy、Macro-F1。
- [x] 混淆矩阵图。
- [x] 分类失败案例：真实类别、预测类别、置信度。

### 检查与验收清单

- [x] 测试集只评估一次最终模型，不参与调参。
- [x] 结果文件中包含 ResNet18 和 EfficientNet-B0 的同口径指标。
- [x] 混淆矩阵维度为 `37 x 37`。
- [x] 报告表格中的指标和 JSON / CSV 文件一致。
- [x] 不得为了表格好看而手工编造或修改指标。

### 第 9 步执行记录（2026-06-02）

已新增 `src/eval_cls.py` 和 `src/summarize_cls_eval.py`，在官方 `test` split 上完成 ResNet18 与 EfficientNet-B0 的最终分类评估。两个 checkpoint 均通过 `load_state_dict(strict=True)` 检查，类别数为 37，输入尺寸为 `224`。

实际评估命令：

```powershell
D:\Anaconda3\envs\unet\python.exe src\eval_cls.py --model resnet18 --split test --batch-size 64 --num-workers 0 --metrics-out outputs\results\cls_metrics_resnet18.json --predictions-out outputs\results\cls_predictions_resnet18_test.csv --confusion-matrix-out outputs\figures\fig_confusion_matrix_resnet18.png
D:\Anaconda3\envs\unet\python.exe src\eval_cls.py --model efficientnet_b0 --split test --batch-size 64 --num-workers 0 --metrics-out outputs\results\cls_metrics_efficientnet_b0.json --predictions-out outputs\results\cls_predictions_efficientnet_b0_test.csv --confusion-matrix-out outputs\figures\fig_confusion_matrix_efficientnet_b0.png --write-canonical-confusion-matrix
D:\Anaconda3\envs\unet\python.exe src\summarize_cls_eval.py --main-model efficientnet_b0 --split test
```

测试集指标：

| 模型 | 测试样本数 | Best epoch | Accuracy | Macro-F1 | Top-5 Accuracy |
| --- | ---: | ---: | ---: | ---: | ---: |
| ResNet18 | 3669 | 15 | 0.883074 | 0.881759 | 0.986645 |
| EfficientNet-B0 | 3669 | 13 | 0.899700 | 0.898173 | 0.995639 |

生成产物：

- 指标 JSON：`outputs/results/cls_metrics_resnet18.json`、`outputs/results/cls_metrics_efficientnet_b0.json`
- 逐样本预测：`outputs/results/cls_predictions_resnet18_test.csv`、`outputs/results/cls_predictions_efficientnet_b0_test.csv`
- 分类结果汇总：`outputs/results/cls_test_summary.csv`
- 每类指标：`outputs/results/cls_per_class_metrics_resnet18.csv`、`outputs/results/cls_per_class_metrics_efficientnet_b0.csv`
- 混淆矩阵：`outputs/figures/fig_confusion_matrix_resnet18.png`、`outputs/figures/fig_confusion_matrix_efficientnet_b0.png`、`outputs/figures/fig_confusion_matrix.png`
- 分类样例：`outputs/figures/fig_cls_correct_examples.png`、`outputs/figures/fig_failure_cases.png`

核对结果：两个模型均使用官方 `test` split，预测明细均为 `3669` 行；两个混淆矩阵均为 `37 x 37`；`outputs/results/cls_test_summary.csv` 与两个 JSON 指标文件中的 Accuracy 和 Macro-F1 一致。

---

## 第 10 步：分割模型最终评估

### 目标

在官方测试集上评估 DeepLabV3-MobileNetV3，得到报告中的最终分割结果。

### 需要做的内容

建议拆成以下小步骤执行：

#### 10.1 加载分割模型

- [x] 加载 DeepLabV3-MobileNetV3 最佳权重：`outputs/checkpoints/best_seg_deeplabv3_mobilenet.pth`。
- [x] 确认输出类别数、输入尺寸和训练时一致：2 类输出，输入尺寸 `320 x 320`，checkpoint 来自第 23 个 epoch。

#### 10.2 测试集像素级推理

- [x] 使用官方 `test` split，共 3669 张图像。
- [x] 将 logits 转换为预测类别 mask，并按方案 B 使用二值前景 mask。
- [x] 评估脚本中对 logits 和标签尺寸做对齐检查，必要时使用双线性插值回标签尺寸后再计算指标。

#### 10.3 分割指标汇总

- [x] 计算 Pixel Accuracy、背景 IoU、前景 IoU、mIoU 和 Dice。测试集结果：Pixel Accuracy = 0.961981，背景 IoU = 0.936630，前景 IoU = 0.913212，mIoU = 0.924921，Dice = 0.954638。
- [x] 保存 JSON / CSV 结果：`outputs/results/seg_metrics_deeplabv3_mobilenet.json`、`outputs/results/seg_summary_deeplabv3_mobilenet_test.csv`、`outputs/results/seg_predictions_deeplabv3_mobilenet_test.csv`。
- [x] 挑选成功样例和失败样例用于报告，并生成 `outputs/figures/fig_seg_predictions.png` 与 `outputs/results/seg_failure_analysis_step10.md`。

1. 推荐命令：

   ```powershell
   D:\Anaconda3\envs\unet\python.exe src\eval_seg.py --checkpoint outputs\checkpoints\best_seg_deeplabv3_mobilenet.pth --model deeplabv3_mobilenet --split test
   ```

2. 输出并保存：

   - Pixel Accuracy
   - 前景 IoU
   - 背景 IoU
   - mIoU
   - Dice

3. 若使用 `ignore_index=255`，指标计算时必须排除忽略像素。

### 需要收集的报告材料

- [x] 分割指标 JSON：`outputs/results/seg_metrics_deeplabv3_mobilenet.json`
- [x] 分割结果表：模型、输入尺寸、Pixel Accuracy、mIoU、Dice，已保存为 `outputs/results/seg_summary_deeplabv3_mobilenet_test.csv`。
- [x] 若记录了不同输入尺寸或 batch size，也可作为补充说明。本次最终评估使用输入尺寸 `320`、batch size `4`。

### 检查与验收清单

- [x] 指标计算不报错，且数值范围在 `[0, 1]`。
- [x] mIoU、Dice 与可视化结果基本一致，不能出现指标很高但图像明显全错的情况。高 mIoU 样例显示模型能稳定覆盖宠物前景；低 mIoU 样例中包含原始 trimap 全背景的标注异常和真实预测失败案例。
- [x] 报告表格中的指标和结果文件一致。`seg_summary_deeplabv3_mobilenet_test.csv` 与 `seg_metrics_deeplabv3_mobilenet.json` 数值一致。
- [x] 对分割边界、复杂背景、遮挡等失败情况有文字分析。当前记录位于 `outputs/results/seg_failure_analysis_step10.md`，并区分了标注异常与真实模型失败。

---

## 第 11 步：生成报告可视化图片

### 目标

生成报告正文需要展示的核心图像材料，让报告不只是文字和表格。

### 需要做的内容

建议拆成以下小步骤执行：

#### 11.1 数据与标签类图片

- [x] 生成数据样例图。
- [x] 生成类别分布图。
- [x] 生成原始 trimap 与二值 mask 对比图。

#### 11.2 训练过程类图片

- [x] 生成分类训练曲线。
- [x] 生成分割训练曲线。
- [x] 曲线中标注 train / val 指标，避免图例不清。

#### 11.3 结果分析类图片

- [x] 生成分类混淆矩阵。
- [x] 生成分割预测对比图。
- [x] 生成失败案例图。

#### 11.4 图片入报告前检查

- [x] 检查分辨率、字体、图例和标题。
- [x] 检查图片路径与 LaTeX 引用路径一致。
- [x] 确认每张图都有对应分析文字。

1. 数据集样例：

   - 文件名：`outputs/figures/fig_dataset_samples.png`
   - 内容：多张宠物原图 + 类别名

2. mask 样例：

   - 文件名：`outputs/figures/fig_mask_samples.png`
   - 内容：原图、原始 trimap、二值 mask

3. 类别分布：

   - 文件名：`outputs/figures/fig_class_distribution.png`
   - 内容：37 类样本数量柱状图

4. 分类训练曲线：

   - 文件名：`outputs/figures/fig_cls_training_curve.png`
   - 内容：train loss、val loss、val accuracy、val macro-F1

5. 分类混淆矩阵：

   - 文件名：`outputs/figures/fig_confusion_matrix.png`
   - 内容：37 类混淆矩阵

6. 分割训练曲线：

   - 文件名：`outputs/figures/fig_seg_training_curve.png`
   - 内容：train loss、val loss、val mIoU、val Dice

7. 分割预测：

   - 文件名：`outputs/figures/fig_seg_predictions.png`
   - 内容：原图、真实 mask、预测 mask、叠加图

8. 失败案例：

   - 文件名：`outputs/figures/fig_failure_cases.png`
   - 内容：分类错误样例或分割失败样例

### 需要收集的报告材料

- [x] 上述 8 张核心图片。
- [x] 每张图片对应的简短说明文字。

### 检查与验收清单

- [x] 图片文件存在，能正常打开。
- [x] 图片分辨率足够用于报告，文字不糊、不重叠。
- [x] 每张图在报告中都有编号、标题和解释。
- [x] 分割预测图至少展示 6 组样例，最好覆盖成功样例和失败样例。
- [x] 失败案例不是随意挑选，应能支撑后续分析。

### 第 11 步执行记录（2026-06-02）

已完成 8 张报告核心图片的文件存在性、分辨率和可读性检查，并在 `main.tex` 中加入对应的 `\includegraphics`、`\caption`、`\label` 和简短分析文字。图片引用均使用当前项目内的相对路径 `outputs/figures/...`。

已核查图片：

- `outputs/figures/fig_dataset_samples.png`：数据集样例图。
- `outputs/figures/fig_class_distribution.png`：训练集类别分布图。
- `outputs/figures/fig_mask_samples.png`：原始 trimap 与二值前景 mask 转换图。
- `outputs/figures/fig_cls_training_curve.png`：ResNet18 与 EfficientNet-B0 分类训练曲线。
- `outputs/figures/fig_confusion_matrix.png`：EfficientNet-B0 测试集混淆矩阵。
- `outputs/figures/fig_seg_training_curve.png`：DeepLabV3-MobileNetV3 分割训练曲线。
- `outputs/figures/fig_seg_predictions.png`：6 组分割预测对比样例，包含高 mIoU 成功样例和低 mIoU 失败样例。
- `outputs/figures/fig_failure_cases.png`：EfficientNet-B0 高置信度分类错误样例。

验证记录：直接执行 `xelatex main.tex` 时因既有 `main.log` 写入权限被拒绝而停止；改用 `xelatex -jobname=step11_visual_check main.tex` 连续编译两次后成功生成 `step11_visual_check.pdf`，说明图片路径、图号引用和 LaTeX 插图语法可用。该权限问题不属于 Step 11 图片路径或图片质量问题，后续 Step 15 正式编译时如仍复现，可关闭占用 `main.log` 的程序或改用新的 jobname 后再生成最终 PDF。

---

## 第 12 步：完成轻量消融实验

### 目标

用最小额外成本完成报告中的对比分析，体现实验设计完整性。

### 推荐方案：ResNet18 vs EfficientNet-B0

这个消融不需要额外训练新任务，直接复用第 6 步和第 7 步结果。

### 需要做的内容

1. 汇总两个模型在测试集上的指标：

   - Accuracy
   - Macro-F1
   - 最佳 epoch
   - 训练耗时，可选

2. 写出对比分析：

   - ResNet18 训练速度和结构简单性
   - EfficientNet-B0 的特征表达能力
   - 两者在易混淆类别上的差异
   - 如果 EfficientNet-B0 未明显优于 ResNet18，需要解释可能原因

### 需要收集的报告材料

- [x] 消融实验表：`ResNet18 vs EfficientNet-B0`。已生成 `outputs/results/cls_ablation_test_summary.csv` 和 `outputs/results/cls_ablation_test_summary.json`。
- [x] 两个模型训练曲线对比图，或在同一张分类曲线图中体现。已生成 `outputs/figures/fig_cls_training_curve.png`。
- [x] 文字分析段落。已整理为 `outputs/results/step12_ablation_analysis.md`。

### 检查与验收清单

- [x] 两个模型使用相同数据划分和相同测试集。均使用随机种子 `42`、官方 `test` split、测试样本数 `3669`、输入尺寸 `224`。
- [x] 对比指标来自真实评估输出。ResNet18 指标来自 `outputs/results/cls_metrics_resnet18.json`，EfficientNet-B0 指标来自 `outputs/results/cls_metrics_efficientnet_b0.json`。
- [x] 报告中至少有一段明确分析，而不是只列数字。已在 `outputs/results/step12_ablation_analysis.md` 中分析整体指标、类别层面差异和主要混淆类别。
- [x] 消融结论与实验结果一致，不强行声称某模型更好。测试集上 EfficientNet-B0 Accuracy / Macro-F1 为 `0.899700 / 0.898173`，ResNet18 为 `0.883074 / 0.881759`；结论表述为 EfficientNet-B0 整体更优，但训练耗时更高，且部分类别仍存在混淆。

---

## 第 13 步：整理实验过程原始记录

### 目标

为报告“实验过程原始记录”章节准备可追溯材料。

### 需要做的内容

整理以下内容：

1. 数据读取记录：

   - 数据集大小
   - 类别数量
   - train / val / test 划分数量

2. mask 转换记录：

   - 原始 trimap 像素值
   - 二值转换规则
   - 转换后 mask 样例

3. 训练记录：

   - ResNet18 训练日志
   - EfficientNet-B0 训练日志
   - DeepLabV3-MobileNetV3 训练日志

4. 模型输出记录：

   - 分类正确样例
   - 分类错误样例
   - 分割成功样例
   - 分割失败样例

### 需要收集的报告材料

- [x] 原始记录表：实验编号、任务、模型、输入尺寸、batch size、epoch、学习率。已生成 `outputs/results/experiment_records_step13.csv` 和 `outputs/results/experiment_records_step13.md`。
- [x] 终端日志截图，可选。当前使用训练 stdout / stderr 文本日志替代截图，ResNet18 日志片段见 `outputs/logs/resnet18_cls_train_stdout.txt`；EfficientNet-B0 和分割训练以 CSV 日志及执行记录为准。
- [x] CSV 日志文件。分类日志为 `outputs/logs/resnet18_cls_log.csv`、`outputs/logs/efficientnet_b0_cls_log.csv`；分割日志为 `outputs/logs/deeplabv3_mobilenet_seg_log.csv`。
- [x] 可视化图片。已生成数据样例、mask 样例、类别分布、分类/分割训练曲线、混淆矩阵、分割预测图、分类正确样例和失败案例图，统一保存在 `outputs/figures/`。

原始记录表采用以下真实运行参数：

| 实验编号 | 任务 | 模型 | 输入尺寸 | batch size | epoch | 学习率 |
| --- | --- | --- | --- | --- | --- | --- |
| Exp-1 | 分类 | ResNet18 | 224 | 32 | 20 | 1e-4 |
| Exp-2 | 分类 | EfficientNet-B0 | 224 | 32 | 25 | 1e-4 |
| Exp-3 | 分割 | DeepLabV3-MobileNetV3 | 320 | 4 | 30 | 1e-4 |

补充记录：

- Exp-1 最佳 epoch 为第 15 轮，最终测试 Accuracy / Macro-F1 为 `0.883074 / 0.881759`。
- Exp-2 最佳 epoch 为第 13 轮，最终测试 Accuracy / Macro-F1 为 `0.899700 / 0.898173`。
- Exp-3 最佳 epoch 为第 23 轮，最终测试 Pixel Accuracy / mIoU / Dice 为 `0.961981 / 0.924921 / 0.954638`。
- 每个实验编号到训练命令、训练日志、最佳权重和测试结果的映射详见 `outputs/results/experiment_records_step13.md`。

### 检查与验收清单

- [x] 每个实验编号能对应到一个训练命令和一个输出结果。Exp-1、Exp-2、Exp-3 均已在 `outputs/results/experiment_records_step13.md` 中建立映射。
- [x] 训练日志、模型权重、结果指标之间能互相对应。三组实验均可由日志、checkpoint 和 JSON / CSV 指标文件互相追溯。
- [x] 原始记录不是流水账，应能支撑结果分析。记录文件同时包含数据读取、mask 转换、训练命令、最终指标和可视化材料清单，可直接支撑报告第 5 章与第 6 章分析。
- [x] 报告中所有图片和表格都能在项目目录中找到源文件。Step 13 记录列出了 `outputs/results/` 和 `outputs/figures/` 中对应源文件。

Step 13 执行记录：

- 已新增 `outputs/results/experiment_records_step13.csv`，用于报告表格引用。
- 已新增 `outputs/results/experiment_records_step13.md`，用于“实验过程原始记录”章节组织材料。
- 已核对真实训练参数并修正原模板中的 epoch 偏差：ResNet18 实际训练 20 epoch、DeepLabV3-MobileNetV3 实际训练 30 epoch。
- 未发现 Step 13 剩余不合规项，可进入第 14 步撰写 LaTeX 报告正文。

---

## 第 14 步：撰写 LaTeX 报告正文

### 目标

基于现有 `main.tex` 模板完成完整实验报告。

### 建议章节安排

1. 实验目的及内容
2. 实验原理及技术路线
3. 所用仪器、材料
4. 实验方法、步骤
5. 实验过程原始记录
6. 实验结果、分析和结论

### 每章需要写入的材料

建议按“先搭骨架、再填材料、最后统一润色”的方式拆分：

#### 14.1 建立报告骨架

- [ ] 保留现有 `main.tex` 模板的文档类、字体、页边距和标题格式。
- [ ] 先创建完整章节标题。
- [ ] 在正文前加入目录，并确保目录能正确显示各级标题。
- [ ] 预留图表位置和标签，避免后期集中调整结构。

#### 14.2 填入方法与过程

- [ ] 将“四、实验方法、步骤”重构为三个二级大节：`实验方法`、`实验步骤`、`实验代码`。
- [ ] 在“实验方法”中集中说明数据划分、预处理、mask 转换、模型设计、训练策略和评估指标。
- [ ] 在“实验步骤”中按实际执行顺序写清楚从数据准备到评估可视化的流程。
- [ ] 在“实验代码”中说明项目代码结构、关键脚本功能和核心运行命令；代码不要整段堆砌，只展示关键函数、命令或伪代码。
- [ ] 插入环境表、数据划分表和训练配置表。
- [ ] 插入数据样例、mask 样例和训练曲线。

#### 14.3 填入结果与分析

- [ ] 插入分类结果表、分割结果表和消融实验表。
- [ ] 插入混淆矩阵、分割预测图和失败案例图。
- [ ] 针对每张图表写分析，不只描述图中数值。

#### 14.4 统一检查全文一致性

- [ ] 检查模型名称、指标名称、图表编号是否前后一致。
- [ ] 检查目录页码和章节标题是否正确更新。
- [ ] 检查报告中的指标是否与 `outputs/results/` 一致。
- [ ] 检查是否仍存在“待填写”等占位内容。

#### 1. 实验目的及内容

- [ ] 写明 Oxford-IIIT Pet 数据集。
- [ ] 写明两个任务：37 类分类和前景分割。
- [ ] 写明迁移学习、模型微调、指标评估和可视化目标。

章节检查清单：

- [ ] 目标明确，不写成泛泛的“学习深度学习”。
- [ ] 能对应后文实际完成的实验。

#### 2. 实验原理及技术路线

- [ ] 图像分类原理。
- [ ] 交叉熵损失。
- [ ] 迁移学习原理。
- [ ] 语义分割原理。
- [ ] DeepLabV3 简要原理。
- [ ] 技术路线流程图或文字流程。

章节检查清单：

- [ ] 每个核心模型或方法都有解释。
- [ ] 公式和文字描述能对应实际代码。
- [ ] 不引入没有实际使用的模型作为主线。

#### 3. 所用仪器、材料

- [ ] 实验环境表。
- [ ] 数据集说明。
- [ ] 软件库说明。

章节检查清单：

- [ ] Python、PyTorch、TorchVision、GPU、CUDA 信息填写完整。
- [ ] 数据集说明包含 37 类和 trimap 标注。

#### 4. 实验方法、步骤

本章建议重构为三个二级大节，避免把方法、流程和代码混在一起写成流水账：

```text
四、实验方法、步骤
  1. 实验方法
  2. 实验步骤
  3. 实验代码
```

##### 4.1 实验方法

这一节写“本实验采用什么方法，以及为什么这样做”，重点解释设计思路，不按命令顺序流水叙述。

- [ ] 数据划分方法：说明使用 Oxford-IIIT Pet 官方 `trainval` 和 `test`，将 `trainval` 按随机种子 `42` 固定划分为训练集和验证集，测试集只用于最终评估。
- [ ] 分类任务方法：说明 37 类宠物品种分类，采用迁移学习，使用 ResNet18 作为 baseline、EfficientNet-B0 作为主模型，二者均加载 ImageNet 预训练权重并替换输出层。
- [ ] 分割任务方法：说明将原始 trimap 转换为宠物前景 / 背景二分类 mask，采用 DeepLabV3-MobileNetV3 完成像素级前景分割。
- [ ] 图像预处理方法：分类输入 `224 x 224`，分割输入 `320 x 320`；说明 ImageNet mean/std 归一化、分类增强、分割图像与 mask 同步增强，以及 mask 最近邻插值。
- [ ] mask 转换方法：说明原始 trimap 中 `1` 为宠物主体、`2` 为背景、`3` 为边界；本实验采用方案 B，即 `1/3 -> 前景 1`，`2 -> 背景 0`。
- [ ] 训练方法：说明损失函数、优化器、学习率、epoch、batch size、最佳模型保存依据；分类按验证集 Macro-F1 保存，分割按验证集 mIoU 保存。
- [ ] 评估方法：分类使用 Accuracy、Macro-F1、Weighted-F1、Top-5 Accuracy、混淆矩阵；分割使用 Pixel Accuracy、背景 IoU、前景 IoU、mIoU、Dice。
- [ ] 可视化方法：说明生成数据样例、类别分布、mask 样例、训练曲线、混淆矩阵、分割预测和失败案例，用于支撑后文分析。

##### 4.2 实验步骤

这一节写“实际按什么顺序完成实验”，要能对应训练命令、输出日志和结果文件。

- [ ] 第一步：确认实验环境和目录结构，使用 `D:\Anaconda3\envs\unet\python.exe` 运行 Python 脚本，输出统一保存到 `outputs/`。
- [ ] 第二步：读取 Oxford-IIIT Pet 数据集，统计 37 类类别、样本数量和 trimap 像素值，生成数据划分文件和类别分布图。
- [ ] 第三步：实现并检查分类和分割预处理流程，确认 dataloader 输出形状、dtype 和 mask 像素值合法。
- [ ] 第四步：实现 trimap 到二值前景 mask 的转换函数，并生成原图、原始 trimap、二值 mask 对比图。
- [ ] 第五步：构建 ResNet18 和 EfficientNet-B0 分类模型，完成前向传播检查。
- [ ] 第六步：训练 ResNet18 分类 baseline，保存训练日志和最佳权重。
- [ ] 第七步：训练 EfficientNet-B0 分类主模型，保存训练日志和最佳权重。
- [ ] 第八步：构建并训练 DeepLabV3-MobileNetV3 分割模型，保存训练日志和最佳权重。
- [ ] 第九步：在官方测试集上评估两个分类模型，保存分类指标、逐样本预测结果和混淆矩阵。
- [ ] 第十步：在官方测试集上评估分割模型，保存分割指标、逐样本预测结果和分割预测图。
- [ ] 第十一步：整理 ResNet18 vs EfficientNet-B0 消融对比，生成对比表和文字分析。
- [ ] 第十二步：整理所有报告图像、实验记录和结果文件，为后续结果分析章节提供材料。

##### 4.3 实验代码

这一节写“代码如何组织、关键代码做了什么、如何运行”，重点展示核心脚本和关键片段，不把完整源码全部粘进报告。

- [ ] 代码结构说明：列出 `src/config.py`、`src/data_transforms.py`、`src/data_loaders.py`、`src/models_cls.py`、`src/models_seg.py`、`src/train_cls.py`、`src/train_seg.py`、`src/eval_cls.py`、`src/eval_seg.py` 等脚本的职责。
- [ ] 配置代码：说明随机种子、数据目录、输入尺寸、batch size、输出目录等集中定义在 `src/config.py`。
- [ ] 数据与预处理代码：展示或概述分类 transform、分割同步 transform、mask 最近邻插值和二值转换函数。
- [ ] 模型构建代码：展示或概述 ResNet18 / EfficientNet-B0 替换分类头，以及 DeepLabV3-MobileNetV3 替换 2 类输出头的关键代码。
- [ ] 训练代码：说明训练循环记录 train loss、val loss、验证指标、学习率和耗时，并按验证指标保存最佳 checkpoint。
- [ ] 评估代码：说明加载 checkpoint、在官方测试集推理、计算指标并保存 JSON / CSV 结果。
- [ ] 可视化代码：说明训练曲线、混淆矩阵、分割预测图和失败案例图由哪些脚本生成。
- [ ] 运行命令：列出三个核心训练命令和两个分类评估、一个分割评估命令，保证报告中的命令与真实执行记录一致。
- [ ] 代码展示原则：报告中只放关键代码片段或命令，完整代码以 `src/` 目录文件为准，避免正文被大段源码淹没。

章节检查清单：

- [ ] “实验方法”回答为什么这样设计，不能只列操作。
- [ ] “实验步骤”按真实执行顺序组织，能追溯到日志、权重和结果文件。
- [ ] “实验代码”说明代码结构和关键实现，避免整段复制所有源码。
- [ ] 关键参数写清楚，包括输入尺寸、batch size、epoch、学习率、优化器、损失函数和最佳模型选择指标。
- [ ] 章节内容与实际运行命令、日志和结果文件一致。

#### 5. 实验过程原始记录

- [ ] 数据样例图。
- [ ] mask 样例图。
- [ ] 训练配置表。
- [ ] 训练曲线。
- [ ] 模型输出样例。

章节检查清单：

- [ ] 至少包含 3 类图像材料：数据样例、训练曲线、模型预测。
- [ ] 日志和曲线来自真实训练。

#### 6. 实验结果、分析和结论

- [ ] 分类结果表。
- [ ] 分割结果表。
- [ ] 消融实验表。
- [ ] 混淆矩阵分析。
- [ ] 分割可视化分析。
- [ ] 失败案例分析。
- [ ] 不足和改进方向。

章节检查清单：

- [ ] 分类和分割都有定量指标。
- [ ] 至少有一个消融对比。
- [ ] 结论包含不足，不只写“效果很好”。
- [ ] 所有指标来自输出文件，不虚构。

---

## 第 15 步：编译报告并修正格式

### 目标

确保最终 PDF 能成功生成，且图片、表格、中文字体和公式排版正常。

### 需要做的内容

1. 使用 `xelatex` 编译：

   ```powershell
   xelatex main.tex
   ```

2. 若引用、目录或交叉引用较多，连续编译两次。

3. 检查：

   - 中文字体是否正常
   - 图片是否缺失
   - 表格是否超出页面
   - 公式是否溢出
   - 章节编号是否符合模板要求
   - 图表标题是否完整

### 需要收集的报告材料

- [ ] 最终 PDF：`main.pdf`
- [ ] 编译日志：`main.log`

### 检查与验收清单

- [ ] `xelatex main.tex` 无致命错误。
- [ ] `main.pdf` 能打开。
- [ ] 图片不缺失、不糊、不超出页面。
- [ ] 表格不明显越界。
- [ ] 关键结果表中的“待填写”全部替换为真实结果。
- [ ] 报告中没有明显乱码。

---

## 第 16 步：最终自检与提交前验收

### 目标

从代码可复现性、结果真实性和报告完整性三个方面完成最终检查。

### 最终检查清单

#### 代码与数据

- [ ] 数据集可读取。
- [ ] `trainval` 固定 8:2 划分。
- [ ] 测试集没有参与训练和调参。
- [ ] 分类训练脚本可运行。
- [ ] 分割训练脚本可运行。
- [ ] 评估脚本可运行。
- [ ] 可视化脚本可运行。
- [ ] 没有写死个人绝对数据路径。

#### 模型与结果

- [ ] 保存了 ResNet18 最佳权重。
- [ ] 保存了 EfficientNet-B0 最佳权重。
- [ ] 保存了 DeepLabV3-MobileNetV3 最佳权重。
- [ ] 有分类测试指标。
- [ ] 有分割测试指标。
- [ ] 有 ResNet18 vs EfficientNet-B0 消融对比。
- [ ] 指标与日志、结果文件一致。

#### 报告材料

- [ ] 有实验环境表。
- [ ] 有数据集划分表。
- [ ] 有分类训练配置表。
- [ ] 有分割训练配置表。
- [ ] 有分类结果表。
- [ ] 有分割结果表。
- [ ] 有消融实验表。
- [ ] 有数据样例图。
- [ ] 有 mask 样例图。
- [ ] 有类别分布图。
- [ ] 有分类训练曲线。
- [ ] 有混淆矩阵。
- [ ] 有分割训练曲线。
- [ ] 有分割预测图。
- [ ] 有失败案例分析。

#### 报告质量

- [ ] 每个关键实验步骤都有解释。
- [ ] 结果分析不是只罗列数字。
- [ ] 结论包含不足和改进方向。
- [ ] 全文没有“待填写”。
- [ ] PDF 可正常打开。
- [ ] 使用 `xelatex` 编译生成。

### 最终验收清单

项目达到以下条件即可视为标准最终版完成：

- [ ] 完成 Oxford-IIIT Pet 的分类和分割两个任务。
- [ ] 完成 ResNet18、EfficientNet-B0、DeepLabV3-MobileNetV3 三个核心模型实验。
- [ ] 至少完成 ResNet18 vs EfficientNet-B0 一个轻量消融。
- [ ] 分类结果包含 Accuracy、Macro-F1、混淆矩阵。
- [ ] 分割结果包含 Pixel Accuracy、mIoU、Dice、mask 可视化。
- [ ] 报告中包含数据处理、模型训练、指标评估、可视化和结果分析。
- [ ] 所有结果来自真实运行输出。
- [ ] `main.tex` 能通过 `xelatex` 编译为最终 PDF。

---

## 推荐执行顺序总览

| 阶段 | 内容 | 主要产物 | 是否必须 |
| --- | --- | --- | --- |
| 0 | 环境检查 | 环境表 | 必须 |
| 1 | 目录和配置 | 目录结构、`config.py` | 必须 |
| 2 | 数据读取与划分 | 数据划分表、类别分布 | 必须 |
| 3 | 预处理与增强 | 数据处理说明 | 必须 |
| 4 | mask 转换 | mask 样例图 | 必须 |
| 5 | 分类模型实现 | ResNet18、EfficientNet-B0 | 必须 |
| 6 | ResNet18 训练 | baseline 权重和日志 | 必须 |
| 7 | EfficientNet-B0 训练 | 主模型权重和日志 | 必须 |
| 8 | 分割模型训练 | 分割权重和日志 | 必须 |
| 9 | 分类评估 | 分类指标、混淆矩阵 | 必须 |
| 10 | 分割评估 | 分割指标 | 必须 |
| 11 | 可视化生成 | 8 类报告图片 | 必须 |
| 12 | 消融实验 | 对比表和分析 | 必须 |
| 13 | 原始记录整理 | 实验记录材料 | 必须 |
| 14 | 报告撰写 | `main.tex` 正文 | 必须 |
| 15 | PDF 编译 | `main.pdf` | 必须 |
| 16 | 最终自检 | 完整提交包 | 必须 |
