# 二、实验原理及技术路线初级方案

本文档用于规划实验报告中“二、实验原理及技术路线”部分的填写内容。该部分建议作为全文实验设计的理论铺垫，重点说明“为什么这样做”和“整体流程是什么”，不要过多展开具体训练命令、日志文件和最终结果分析，这些内容应主要放在后续“实验方法、步骤”“实验过程原始记录”和“实验结果、分析和结论”中。

本节二级标题建议保留为以下三个部分：

1.概述
2.实验原理
3.技术路线

在正式写入 `main.tex` 时，可以对应为：

```latex
\subsection{概述}
\subsection{实验原理}
\subsection{技术路线}
```

其中，“实验原理”下面可以使用三级标题展开模型和指标原理，例如：

```latex
\subsubsection*{2.1 图像分类原理}
\subsubsection*{2.2 迁移学习与预训练模型}
\subsubsection*{2.3 语义分割与前景 mask}
\subsubsection*{2.4 损失函数与评价指标}
```

## 1.概述

### 写作定位

“概述”用于简要交代本实验的研究对象、任务目标和整体思路。建议控制在 1 到 2 个自然段，不展开复杂公式和模型细节。

本实验围绕 Oxford-IIIT Pet 数据集开展宠物图像理解任务，主要包括两个方向：一是 37 类宠物品种分类，二是宠物前景二分类语义分割。分类任务关注整张图像所属的宠物品种，分割任务关注图像中每个像素属于宠物前景还是背景。二者共同构成从图像级识别到像素级理解的综合计算机视觉实验。

实验总体采用迁移学习思想。分类部分以 ResNet18 作为 baseline，并以 EfficientNet-B0 作为主要分类模型；分割部分使用 DeepLabV3-MobileNetV3 进行宠物前景提取。实验流程包括数据集读取、固定随机划分、图像预处理、模型微调训练、测试集评估、可视化生成和结果分析。

### 可写要点

- 数据集：Oxford-IIIT Pet。
- 分类任务：37 类宠物品种分类。
- 分割任务：将 trimap 标注转换为背景/宠物前景二分类 mask。
- 分类模型：ResNet18 baseline、EfficientNet-B0 主模型。
- 分割模型：DeepLabV3-MobileNetV3。
- 方法主线：迁移学习、统一预处理、固定划分、指标评估和可视化分析。

## 2.实验原理

### 2.1 图像分类原理

图像分类的目标是将输入图像映射到预定义类别集合中。本实验的分类任务共有 37 个宠物品种类别，因此分类模型最后输出 37 维 logits。经过 softmax 后，每一维可以理解为图像属于对应类别的预测概率，最终预测类别通常取概率最大的类别。

可以在正文中说明：分类模型首先通过卷积层和特征提取模块获得图像的局部纹理、边缘、形状和语义特征，再通过全局池化和全连接分类层输出类别得分。对于宠物品种分类而言，模型需要学习毛发颜色、面部结构、耳朵形状、体型等差异特征。

### 2.2 迁移学习与预训练模型

迁移学习是本实验分类和分割模型训练的重要基础。由于 Oxford-IIIT Pet 数据集规模相对有限，若从零开始训练深层神经网络，容易出现收敛慢、泛化能力不足等问题。因此，本实验使用在 ImageNet 上预训练的模型权重作为初始化，再根据宠物分类和前景分割任务修改输出层并进行微调。

分类部分使用 ResNet18 和 EfficientNet-B0。ResNet18 通过残差连接缓解深层网络训练中的梯度退化问题，适合作为结构清晰、实现稳定的 baseline；EfficientNet-B0 通过复合缩放思想平衡网络深度、宽度和输入分辨率，在较小模型规模下通常具有较好的分类性能。

分割部分使用 DeepLabV3-MobileNetV3。DeepLabV3 通过空洞卷积和多尺度上下文建模增强像素级语义理解能力，MobileNetV3 作为轻量化 backbone 能降低计算量，适合在课程实验环境中完成训练和评估。

### 2.3 语义分割与前景 mask

语义分割与图像分类不同，它不是为整张图像预测一个类别，而是为图像中的每个像素预测类别。本实验的分割任务不区分宠物的具体品种，只判断每个像素属于背景还是宠物前景，因此属于二分类语义分割任务。

Oxford-IIIT Pet 数据集提供 trimap 标注。原始 trimap 中包含宠物主体、边界区域和背景区域。本实验为了简化任务，将 trimap 转换为二值 mask：背景映射为 0，宠物主体和边界区域统一映射为前景 1。这样模型输出通道数可以设置为 2，对应背景和宠物前景两个类别。

该部分可以强调：mask 处理时应保持像素级标签的离散性，空间变换需要保证图像和 mask 对齐；对 mask 调整尺寸时通常使用最近邻插值，避免产生非法类别值。

### 2.4 损失函数与评价指标

分类和分割训练均可以使用交叉熵损失。分类任务中，交叉熵衡量模型预测类别分布与真实品种标签之间的差异；分割任务中，交叉熵在像素级别计算，用于衡量每个像素的预测类别与真实 mask 标签之间的差异。

分类任务建议说明 Accuracy、Macro-F1、Weighted-F1 和 Top-5 Accuracy 的含义。Accuracy 反映整体分类正确率；Macro-F1 对每个类别一视同仁，能够体现类别平均表现；Weighted-F1 根据类别样本数加权，更接近整体样本分布；Top-5 Accuracy 用于观察真实类别是否位于模型预测概率最高的前 5 个类别中。

分割任务建议说明 Pixel Accuracy、IoU、mIoU 和 Dice 的含义。Pixel Accuracy 衡量像素级整体正确率；IoU 衡量预测区域与真实区域的交并比；mIoU 是各类别 IoU 的平均值；Dice 更关注预测前景区域与真实前景区域的重合程度。

### 建议避免的内容

- 不在本节提前写最终测试指标数值。
- 不详细描述每个训练脚本的命令和参数。
- 不展开大量代码说明。
- 不对具体失败样例和混淆类别做深入分析。

这些内容更适合放在第四节、第五节和第六节中。

## 3.技术路线

### 3.1 整体流程

技术路线建议用“文字说明 + 流程图”的形式呈现。正文先概括实验从数据到结果的主流程，再用图示展示各模块之间的关系。

推荐流程如下：

1. 读取 Oxford-IIIT Pet 数据集。
2. 使用固定随机种子划分训练集、验证集和测试集。
3. 对分类任务构建图像预处理和数据增强流程。
4. 对分割任务完成图像预处理和 trimap 到二值 mask 的转换。
5. 训练 ResNet18 baseline 分类模型。
6. 训练 EfficientNet-B0 主分类模型。
7. 训练 DeepLabV3-MobileNetV3 前景分割模型。
8. 在测试集上分别评估分类模型和分割模型。
9. 生成训练曲线、混淆矩阵、分类样例和分割可视化图。
10. 汇总指标和图表，完成结果分析和实验报告撰写。

### 3.2 分类任务路线

分类路线可以单独概括为：

```text
原始图像 -> 图像缩放与归一化 -> 数据增强 -> 预训练分类模型 -> 37 类输出 -> 分类指标与混淆矩阵
```

正文可以写为：

分类任务中，首先读取图像及其品种标签，并将图像统一处理为模型输入尺寸。训练阶段加入适当的数据增强，提高模型对姿态、位置和局部变化的适应能力。随后分别使用 ResNet18 和 EfficientNet-B0 进行迁移学习训练，最后在测试集上计算 Accuracy、Macro-F1、Weighted-F1 和 Top-5 Accuracy，并绘制混淆矩阵和样例预测图。

### 3.3 分割任务路线

分割路线可以单独概括为：

```text
原始图像 + trimap -> 图像与 mask 同步变换 -> 二值前景 mask -> DeepLabV3-MobileNetV3 -> 像素级预测 -> mIoU / Dice 与可视化
```

正文可以写为：

分割任务中，首先读取图像及其 trimap 标注，并将 trimap 转换为背景和宠物前景二分类 mask。图像和 mask 在预处理时需要保持空间对应关系。模型使用 DeepLabV3-MobileNetV3 输出每个像素的类别预测，并在测试集上计算 Pixel Accuracy、mIoU 和 Dice 等指标，同时生成原图、真实 mask 和预测 mask 的对比图。

### 3.4 报告中可使用的流程图草案

如果后续需要在 `main.tex` 中绘制简洁流程图，可以先使用文本框方式，避免引入额外绘图包：

```latex
\begin{figure}[H]
\centering
\fbox{\parbox{0.9\textwidth}{
\centering
Oxford-IIIT Pet 数据读取 $\rightarrow$ 固定随机划分 $\rightarrow$ 图像预处理与增强 \\
$\rightarrow$ 分类模型训练与评估 $\rightarrow$ 分割 mask 转换与模型训练 \\
$\rightarrow$ 测试集指标计算 $\rightarrow$ 可视化结果生成 $\rightarrow$ 结果分析与报告撰写
}}
\caption{实验技术路线}
\label{fig:pipeline}
\end{figure}
```

如果希望技术路线更清晰，也可以在后续修改为 TikZ 流程图或分成“分类路线”和“分割路线”两条并行分支。

## 后续讨论点

- “实验原理”是否需要保留数学公式，例如交叉熵、IoU 和 Dice 的定义。
- ResNet18、EfficientNet-B0、DeepLabV3-MobileNetV3 的介绍详略程度是否需要压缩。
- 技术路线图是使用当前的文本框形式，还是改成更正式的流程图。
- 是否将分类路线和分割路线画成两条并行分支，以突出两个任务的关系。
