# 第四节“实验方法、步骤”写作规划

## 章节定位

第四节用于说明实验是如何设计、如何执行、代码如何组织的。该节重点回答“用什么方法做”“按什么顺序做”“代码在哪里、关键代码做了什么”，不展开最终结果分析。最终指标、曲线解读和失败案例应主要放在第五节“实验过程原始记录”和第六节“实验结果、分析和结论”。

考虑后续需要加入目录，本节建议在正文中手动采用更清晰的层级编号：

- 四、实验方法、步骤
- 1. 实验方法
- 1.1 数据划分方法
- 1.2 图像预处理与 mask 转换方法
- 1.3 模型训练方法
- 1.4 评价与可视化方法
- 2. 实验步骤
- 3. 实验代码
- 3.1 数据处理模块重要代码
- 3.2 分类模型模块重要代码
- 3.3 分割模型模块重要代码
- 3.4 训练与评估模块重要代码

在 `main.tex` 中可以继续使用现有模板的 `\section` 和 `\subsection`，但建议在小标题文字中写入编号，例如：

```latex
\section{实验方法、步骤}
\subsection{实验方法}
\subsubsection*{1.1 数据划分方法}
\subsubsection*{1.2 图像预处理与 mask 转换方法}
```

如果后续希望这些 `1.1` 小节也进入目录，可以进一步改成可加入目录的写法，例如配合 `\addcontentsline`。

## 1. 实验方法

本小节写“实验设计方案”，说明分类任务和分割任务分别采用什么方法，以及关键处理口径。建议按 `1.1`、`1.2`、`1.3`、`1.4` 展开。

### 1.1 数据划分方法

应写内容：

- 使用 TorchVision `OxfordIIITPet` 数据集接口读取 Oxford-IIIT Pet 数据集。
- 分类任务读取 37 类宠物品种标签，分割任务读取 trimap 标注。
- 官方 `trainval` split 按固定随机种子 42 划分为训练集和验证集。
- 官方 `test` split 只用于最终测试，不参与训练和模型选择。
- 划分索引保存到 `outputs/results/split_indices_seed42.json`，保证可复现。

写作目的：

- 说明数据划分没有随机漂移。
- 说明验证集和测试集职责不同，避免“测试集参与调参”的问题。

可引用材料：

- `src/inspect_data.py`
- `src/data_loaders.py`
- `outputs/results/data_summary.json`
- `outputs/results/split_indices_seed42.json`

### 1.2 图像预处理与 mask 转换方法

应写内容：

- 分类输入尺寸设为 `224 x 224`，使用 ImageNet mean/std 归一化。
- 分类训练集使用轻量数据增强，验证集和测试集使用确定性预处理。
- 分割输入尺寸设为 `320 x 320`。
- 分割图像 resize 使用双线性插值，mask resize 使用最近邻插值。
- trimap 转二值 mask 的规则为：原始值 2 映射为背景 0，原始值 1 和 3 映射为前景 1。
- 对图像和 mask 同步应用空间变换，保证像素级标签对齐。

写作目的：

- 说明分类和分割的数据处理不同。
- 说明为什么 mask 不能使用普通图像插值。
- 说明二分类前景分割的标签定义。

可引用材料：

- `src/data_transforms.py`
- `src/segmentation_utils.py`
- `src/inspect_preprocessing.py`
- `src/inspect_step4_masks.py`
- `outputs/results/preprocessing_check.json`
- `outputs/results/mask_conversion_check.json`

### 1.3 模型训练方法

应写内容：

- 分类任务采用迁移学习。
- `ResNet18` 作为 baseline，`EfficientNet-B0` 作为主分类模型。
- 两个分类模型均加载 ImageNet 预训练权重，并将输出层替换为 37 类。
- 分割任务使用 `DeepLabV3-MobileNetV3`，将分割头输出类别数改为 2。
- 分类和分割均使用交叉熵损失。
- 优化器使用 AdamW。
- 训练过程记录 loss、验证指标、学习率和耗时。
- 分类最佳模型按验证集 Macro-F1 优先保存，Macro-F1 相同再比较 Accuracy。
- 分割最佳模型按验证集 mIoU 或 Dice 相关指标保存，具体以脚本实际配置为准。

写作目的：

- 说明 baseline、主模型和分割模型的职责。
- 说明模型修改点和训练选择依据。
- 说明为什么用 Macro-F1、mIoU、Dice 等指标参与模型选择。

可引用材料：

- `src/models_cls.py`
- `src/models_seg.py`
- `src/train_cls.py`
- `src/train_seg.py`
- `outputs/logs/`
- `outputs/checkpoints/`

### 1.4 评价与可视化方法

应写内容：

- 分类测试指标包括 Accuracy、Macro-F1、Weighted-F1、Top-5 Accuracy、每类 precision / recall / F1。
- 分类可视化包括训练曲线、混淆矩阵、正确样例和错误样例。
- 分割测试指标包括 Pixel Accuracy、背景 IoU、前景 IoU、mIoU 和 Dice。
- 分割可视化包括原图、真实 mask、预测 mask 的对比图，以及失败案例。
- 所有结果应来自真实运行输出文件，不手工编造指标。

写作目的：

- 说明评价指标覆盖总体表现、类别均衡表现和像素级表现。
- 说明后续第五节和第六节中的图表来源。

可引用材料：

- `src/eval_cls.py`
- `src/eval_seg.py`
- `src/plot_cls_training_curve.py`
- `src/plot_seg_training_curve.py`
- `src/visualize_seg_predictions.py`
- `src/summarize_cls_eval.py`
- `outputs/results/cls_metrics_efficientnet_b0.json`
- `outputs/results/cls_metrics_resnet18.json`
- `outputs/results/seg_metrics_deeplabv3_mobilenet.json`

## 2. 实验步骤

本小节不需要继续拆很多小标题，建议用有序列表写清楚实验顺序。每一步只说明做了什么、产出什么文件或图表，不在这里展开结果分析。

建议写作顺序：

1. 准备 Python 环境和项目目录，确认使用 conda 环境 `unet`，并统一从项目根目录运行脚本。
2. 检查 Oxford-IIIT Pet 数据集是否可读取，统计样本数量、类别数量、类别分布和 trimap 像素值。
3. 使用固定随机种子 42 将官方 `trainval` 划分为训练集和验证集，保留官方 `test` 作为最终测试集，并保存划分索引。
4. 检查分类图像预处理、分割图像预处理和 trimap 转二值 mask 规则，确认图像与 mask 的空间变换保持一致。
5. 训练 ResNet18 baseline 分类模型，保存训练日志和验证集最优权重。
6. 训练 EfficientNet-B0 主分类模型，保存训练日志和验证集最优权重。
7. 训练 DeepLabV3-MobileNetV3 前景分割模型，保存训练日志和验证集最优权重。
8. 在官方测试集上评估 ResNet18 和 EfficientNet-B0，生成分类指标、逐样本预测结果和混淆矩阵。
9. 在官方测试集上评估 DeepLabV3-MobileNetV3，生成分割指标、逐样本预测结果和可视化样例。
10. 绘制训练曲线、分类正确样例、分类错误样例、分割预测对比图和失败案例图。
11. 汇总实验记录文件，为第五节原始记录和第六节结果分析提供依据。
12. 使用 `xelatex main.tex` 编译报告，检查图表编号、路径、目录和排版。

写作注意：

- 不要把每一步再拆成小标题。
- 不要在步骤里提前写“模型效果很好”等结论。
- 如果出现具体指标，应确保来自 `outputs/results/` 中的真实文件。

## 3. 实验代码

本小节先用表格交代代码模块，再按 `3.1`、`3.2`、`3.3` 展示关键代码。每段代码展示前必须先说明“这段代码的作用是什么”，避免只堆代码。

### 代码模块表

| 模块 | 主要文件 | 作用 |
| --- | --- | --- |
| 配置模块 | `src/config.py` | 统一保存路径、随机种子、训练超参数和设备配置 |
| 数据处理模块 | `src/data_loaders.py`、`src/data_transforms.py`、`src/segmentation_utils.py` | 构建数据集、定义预处理和数据增强、完成 trimap 到二值 mask 的转换 |
| 分类模型模块 | `src/models_cls.py` | 构建 ResNet18 baseline 和 EfficientNet-B0 主分类模型 |
| 分割模型模块 | `src/models_seg.py` | 构建 DeepLabV3-MobileNetV3 前景二分类分割模型 |
| 训练模块 | `src/train_cls.py`、`src/train_seg.py` | 执行分类模型和分割模型训练，保存日志与最优权重 |
| 评估模块 | `src/eval_cls.py`、`src/eval_seg.py` | 在测试集上计算分类和分割指标，保存预测结果 |
| 可视化模块 | `src/plot_cls_training_curve.py`、`src/plot_seg_training_curve.py`、`src/visualize_seg_predictions.py` | 绘制训练曲线、混淆矩阵、分类样例和分割预测图 |
| 结果汇总模块 | `src/summarize_cls_eval.py` | 汇总分类评估结果，生成报告可用表格材料 |

### 3.1 数据处理模块重要代码

代码作用：

这一部分重点展示数据读取、固定划分、分类预处理、分割预处理和 trimap 转二值 mask。建议优先展示 trimap 转换函数，因为它直接决定分割任务的标签定义。

建议展示代码：

- `src/segmentation_utils.py` 中 trimap 转二值 mask 的函数。
- `src/data_transforms.py` 中分类和分割 transform 的定义。
- `src/data_loaders.py` 中固定划分 train / val / test 的逻辑。

写作方式：

先说明 Oxford-IIIT Pet 原始 trimap 包含 1、2、3 三类像素值，再说明本实验将其转换为前景和背景二分类 mask。随后放代码片段，并解释最近邻插值用于 mask resize，是为了避免产生非法类别值。

### 3.2 分类模型模块重要代码

代码作用：

这一部分展示如何基于 TorchVision 预训练模型完成迁移学习。重点不是展示完整网络结构，而是说明如何加载预训练权重、替换最后输出层，并让模型输出 37 类宠物品种。

建议展示代码：

- `src/models_cls.py` 中构建 ResNet18 的函数。
- `src/models_cls.py` 中构建 EfficientNet-B0 的函数。
- 最后一层替换为 37 类输出的代码。

写作方式：

先说明 ResNet18 用作 baseline，EfficientNet-B0 用作主分类模型。然后展示模型构建代码，重点解释输出层维度从 ImageNet 的 1000 类改为本任务的 37 类。

### 3.3 分割模型模块重要代码

代码作用：

这一部分展示如何将 DeepLabV3-MobileNetV3 改造成宠物前景二分类分割模型。重点是说明模型输入为图像，输出为每个像素属于背景或前景的 logits。

建议展示代码：

- `src/models_seg.py` 中构建 DeepLabV3-MobileNetV3 的函数。
- 修改 classifier 输出通道为 2 类的代码。
- 必要时展示前向输出中 `out` 字段的使用方式。

写作方式：

先说明 Oxford-IIIT Pet 的分割任务在本实验中被定义为“背景 / 宠物前景”二分类问题。然后展示分割模型构建代码，并解释输出通道数为 2 对应背景和前景。

### 3.4 训练与评估模块重要代码

代码作用：

这一部分展示模型训练、最佳权重保存和测试指标计算的关键逻辑。重点说明训练过程如何记录指标，以及最终测试结果如何从脚本真实输出得到。

建议展示代码：

- `src/train_cls.py` 中训练一个 epoch、验证和保存最佳分类模型的关键逻辑。
- `src/train_seg.py` 中分割训练、验证和保存最佳模型的关键逻辑。
- `src/eval_cls.py` 中 Accuracy、Macro-F1、Top-5 Accuracy 等指标计算逻辑。
- `src/eval_seg.py` 中 Pixel Accuracy、IoU、mIoU 和 Dice 的计算逻辑。

写作方式：

先说明分类模型以验证集 Macro-F1 作为主要模型选择依据，分割模型以像素级指标作为主要评价依据。然后分别展示训练保存和指标计算代码，强调报告中的结果表格必须来自 `outputs/results/` 下的真实运行文件。

## 第四节与后续章节的边界

第四节应该写：

- 实验设计。
- 执行流程。
- 代码模块表。
- 关键代码作用和片段。
- 指标计算口径。

第四节不应重点写：

- 大量训练日志原文。
- 最终结果表格的详细分析。
- 失败案例的具体类别解释。
- 模型优劣的最终结论。

这些内容建议放到：

- 第五节“实验过程原始记录”：放数据样例、训练曲线、输出样例、记录索引。
- 第六节“实验结果、分析和结论”：放指标表格、模型对比、失败案例分析、结论和改进方向。

## 可直接迁移到 LaTeX 的小节骨架

```latex
\section{实验方法、步骤}

\subsection{实验方法}

\subsubsection*{1.1 数据划分方法}

\subsubsection*{1.2 图像预处理与 mask 转换方法}

\subsubsection*{1.3 模型训练方法}

\subsubsection*{1.4 评价与可视化方法}

\subsection{实验步骤}

\subsection{实验代码}

\subsubsection*{3.1 数据处理模块重要代码}

\subsubsection*{3.2 分类模型模块重要代码}

\subsubsection*{3.3 分割模型模块重要代码}

\subsubsection*{3.4 训练与评估模块重要代码}
```
